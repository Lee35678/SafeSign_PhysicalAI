# SafeSign PhysicalAI 개발 로그 (Dev Log)

- 프로젝트: SafeSign PhysicalAI ("심기일전") — 산업 안전 수신호 교육용 피지컬 AI 제품
- 작성자: 송승호 (하드웨어·로봇동작 R, services/actuation·services/picar 담당)
- 최초 작성일: 2026-09-21
- 최근 갱신일: 2026-09-23

## 📌 프로젝트 개요

- **목적**: AI Hand(로봇손)가 수신호를 시범 → 학습자가 따라 함 → 카메라+MediaPipe+SVM으로 인식/판정 →
  AI Hand 재시범·picar 주행+LED·micro:bit LED 3개 물리 장치로 피드백을 주는 폐루프 안전교육 제품.
  화면 텍스트가 아니라 실제 로봇 동작으로 피드백을 준다는 점이 핵심 차별점.
- **기술 스택**: Python(FastAPI), TypeScript(MakeCode/micro:bit), MediaPipe HandLandmarker, SVM,
  BLE(`bleak`), Docker Compose. 4인 팀(vision/actuation/picar/web/data 서비스 분담).
- **관련 링크**: `document/10_PRD_v2.md`(종합 기획서), `shared/schemas/`(서비스 간 인터페이스 계약),
  `document/11_하드웨어설계서_v1.md`(하드웨어 단일 출처), `services/actuation/doc/`(micro:bit 디버깅 이력)

## 📊 현재 상태 요약

- 전체 진척률: 설계 문서는 거의 확정 단계. mock 기준으로는 web↔vision↔actuation↔picar 파이프라인이
  end-to-end로 연결됐다. picar 모터 I2C 구현도 완료(제작사 이미지 분석으로 프로토콜 확증, 2026-09-22)되어
  **남은 것은 실물 검증**이다. 웹 프론트엔드는 아직 스캐폴딩 단계.
- 상태: 🟢 **picar 하드웨어 검증 완료 (2026-09-23)** — 전 부품 보유 + 1차 벤치·2차 부하 테스트
  **전 항목 통과**. 전원 단일 계통이 부하에서도 안전하고(`get_throttled=0x0`), 무선 653회 실패
  0건, 지연 P95 43~49ms(예산 500ms). 부하 테스트 중 **정지 실패 결함을 발견해 조치**했고
  재시행만 남았다. 속도 실측값도 확정(서행 20 / 좌우회전·후진 40).
  actuation의 BLE 제스처 재현과 mock 파이프라인은 🟢 정상. **엄지 서보는 교체하지 않기로 확정**되어
  더 이상 블로커가 아니다(결정 사항 참고).
- 다음 마일스톤: W4 테스트/KPI 실측 (~10/04). 선행 조건은 ① 자체 촬영 데이터 확보(KPI 실측의 유일한
  경로, `회의안건_2026-09-21` 안건 1) ② 실물 micro:bit+RPi5에서 `MOCK_HARDWARE=false` 검증

---

## 🗓️ 로그

### 2026-09-21

**작업 내용**
- 엄지 서보(Hiwonder LFD-01) 스왑 테스트로 하드웨어 고장 최종 확정 (`services/actuation/doc/aihand_gesture_checklist_final.md` 세션 11)
- `services/actuation` FastAPI 레이어를 BLE 기반으로 재구성 — main 브랜치의 USB 시리얼 기반 앱이
  BLE 전환 과정에서 통째로 삭제되어 있던 것을 대체 (`src/app.py`, `src/aihand/controller.py`,
  `src/microbit/ble_bridge.py`)
- `target_signal`(정지/서행/...) → micro:bit `G1`~`G7` 제스처 매핑 테이블 작성
- MediaPipe 연동 준비용 통합 테스트 작성·실행 (`tests/vision_to_command_integration_test.py`) —
  vision의 JudgmentResult 형태 값을 `/command`로 흘려보내 7개 수신호 매핑 전부 로컬 검증 완료
- Dockerfile 임포트 버그 발견·수정: `uvicorn src.app:app`이 실제로 `ModuleNotFoundError`를 내는 것을
  재현 확인 후 `uvicorn app:app --app-dir src`로 수정
- `hardware_spec.md`/`hardware_pinmap.md` 소재 확인(`document/`가 아니라 `services/actuation/doc/`에
  있었음) 후 근거 반영: micro:bit 보드 최대 공급 전류(~300mA) vs 손가락 서보 stall 전류(최대 700mA)
  수치로 "손가락 동시 구동 금지" 설계 근거를 코드/README에 문서화
- `services/picar/src/controller.py`의 `PIN_MAP` 재설계 — Raspbot_pinmap 표 기준으로 모터는
  I2C(SCL=BCM3/SDA=BCM2) 경유 구조로 정정, LED 적색(BCM21) 확정, 황색 2개는 외부 배선 예정으로 TODO 표시
- micro:bit 펌웨어 `aihand_production.ts`(운영용)와 `aihand_named_control.ts`(캘리브레이션용)를
  `aihand_control.ts` 하나로 통합 — 최상단 `const TEST_MODE = false;` 한 줄만 바꿔서 운영(`G1`~`G7`
  경량 명령)/테스트(`IDX:`/`HAND:`/`G:` 캘리브레이션 명령) 모드를 전환하도록 재구성. PC 쪽 테스트
  스크립트도 동일한 방식으로 `aihand_production_pc.py`+`aihand_named_control_pc.py` →
  `aihand_control_pc.py`로 통합 (`tests/`)
- micro:bit LED 매트릭스에 판정 결과(RESULT) 표시 기능 구현 — BLE로 `"correct"` 수신 시 O 모양,
  `"incorrect"` 수신 시 X 모양을 2초간 표시 후 꺼짐. `TEST_MODE` 값과 무관하게 두 모드 모두에서
  처리되도록 BLE 명령 분기 최상단에 배치. `aihand_control_pc.py`에도 `correct`/`incorrect` 테스트
  입력 추가. 이후 `ble_bridge.send_result()` 추가 + FastAPI `/result`가 `is_correct`를 그대로 BLE로
  전송하도록 연동까지 완료
- 위 통합·기능 추가에 맞춰 `services/actuation/README.md`와 관련 코드 주석(`app.py`,
  `controller.py`, `ble_bridge.py`)의 옛 파일명(`aihand_production.ts` 등) 참조를 `aihand_control.ts`
  기준으로 갱신
- PROGRESS 프로토콜 구현 — 펌웨어가 `"P<current><total>"`(둘 다 한 자리)을 받아 `"OKP<c><t>"`로 회신.
  **LED 표시는 하지 않는다**(진행 표시는 web 화면 담당으로 결정). `ble_bridge.send_progress()` +
  `/progress` 엔드포인트 연동까지 완료
- `services/web` 상태머신 연동 완료 (`backend/state_machine.py`) — 백그라운드 스레드가 vision
  `GET /latest`를 0.2초 간격으로 폴링해 커리큘럼 단계와 비교하고, actuation `/command`·`/result`·
  `/progress`와 picar `/picar`로 분배한다. picar는 500ms 타임아웃+1회 재시도 후 실패해도 학습 흐름을
  막지 않는다(03_인터페이스계약서_v2 §7). 같은 판정이 연속으로 들어올 때 물리 피드백이 반복 트리거되지
  않도록 `_last_dispatched` 가드 적용
- `shared/schemas/microbit_protocol.md`를 BLE 기준으로 갱신 (`document/03_인터페이스계약서_v2.md`
  §5-3과 함께) — UUID 반전, `G{n}`/`correct`/`incorrect`/`P<c><t>` 명령 표 반영
- `document/10_PRD_v2.md` §3.2 우회전_유도 설명을 펌웨어 기준인 "엄지+소지 펴기"로 정정 완료
- vision 서비스 Dockerfile 임포트 경로 점검 — 이미 `uvicorn app:app --app-dir src`로 되어 있어
  actuation과 같은 버그는 없음을 확인 (의심 이슈 종료)

**이슈/블로커**
- 모터 I2C 명령 프로토콜(레지스터 주소/바이트 포맷) 미확보 — Raspbot 벤더 라이브러리/문서 필요.
  `services/picar`의 실물 경로는 현재 `NotImplementedError`
- 실물 부품(Pi Camera Module 3 · picar · RPi4B) 미도착 — vision/picar는 mock으로만 검증 가능
- 공개 데이터 기준 분류 성능이 KPI 미달(정답률 77.6% / Macro F1 0.784 / 치명 오분류 3건).
  KPI 실측은 자체 촬영 데이터로만 가능한데 촬영 일정이 미정 (`회의안건_2026-09-21` 안건 1)
- 엄지 서보 하드웨어 고장은 **교체하지 않는 것으로 확정**되어 블로커에서 제외 (결정 사항 참고)

**결정 사항**
- 우회전_유도(G4) 매핑: PRD 문서("엄지+약지")와 실제 펌웨어("엄지+소지")가 불일치하는 것을 발견 —
  **펌웨어 쪽을 최종 확정판으로 채택**(팀 확인 완료), PRD 문서 수정은 별도 TODO로 남김
- 황색 LED 2개: Raspbot 보드 내장 LED는 적색·청색 2개뿐이라, 청색을 대용하지 않고 **외부 LED를
  여유 GPIO에 추가 배선**하기로 결정
- **엄지 서보: 교체하지 않는다(확정).** 다만 **엄지 동작 자체는 설계·시현에 그대로 유지**한다 —
  펌웨어 `aihand_control.ts`의 `G1`~`G7` 엄지 각도는 손대지 않고, 물리적으로만 엄지가 움직이지 않는
  상태로 시현한다. 따라서 한때 검토했던 "엄지 제외에 따른 4손가락 제스처 재설계"는 **채택하지 않는다**
  (`G3`↔`G6`, `G4`↔`G7`가 엄지값만 달라 AI Hand 상에서 구별되지 않는 점은 시현상의 제약으로 감수).
  손모양을 바꾸면 PRD §3.2·학습 클래스·공개 데이터까지 연쇄 수정해야 하는 비용이 더 크다는 판단

**다음 할 일**
- [x] ~~`services/web` 상태머신이 vision `/latest` → actuation `/command`를 실제로 호출하도록 연동~~
- [x] ~~`/result`의 `correct`/`incorrect` BLE 전송 연동~~
- [x] ~~micro:bit 펌웨어 PROGRESS 프로토콜 구현~~ (LED 표시 없이 회신만)
- [x] ~~`document/10_PRD_v2.md` §3.2 우회전_유도를 "엄지+소지"로 정정~~
- [x] ~~`shared/schemas/microbit_protocol.md` BLE 기준 갱신~~
- [x] ~~vision 서비스 Dockerfile 임포트 경로 확인~~ → 이미 `--app-dir src`로 정상
- [x] ~~엄지 서보 교체~~ → **교체하지 않기로 확정**, 엄지 동작은 설계·시현에 유지 (결정 사항 참고)
- [ ] 실물 micro:bit + RPi5에서 `MOCK_HARDWARE=false`로 `/command` end-to-end 검증
- [ ] `docker compose up actuation`으로 RPi5 실물 환경에서 BLE(BlueZ/D-Bus) 접근 검증
- [ ] 모터 I2C 명령 프로토콜 확보 (Raspbot 벤더 라이브러리/문서)
- [ ] 황색 LED 2개 배선 및 최종 BCM 핀 확정
- [ ] RPi5(4GB) 런타임 리소스 실측 — vision/actuation/web 3개 컨테이너 동시 구동 시 `docker stats`로
  메모리/CPU 확인, MediaPipe 프레임 처리율(fps) 실측, 액티브 쿨러 장착 여부 확인 (08_리스크레지스터 신규
  항목 참고)

### 2026-09-21 (추가) — A-1 실물 BLE 검증

**작업 내용**
- A-1(실물 micro:bit `MOCK_HARDWARE=false` 검증) 수행. RPi5 대신 Windows PC에서 진행했다 — `bleak`이
  크로스플랫폼이라 BLE 구간 검증은 동일하게 성립하고, 컨테이너/BlueZ 관련 항목만 RPi5로 미뤘다.
  - BLE 스캔: `BBC micro:bit [zezuz]`(`E5:4C:EF:73:24:A9`) 탐지 확인
  - `tests/ble_debug_services.py`로 UUID 실측 대조 — `6e400003`=write(RX), `6e400002`=indicate(TX)로
    `ble_bridge.py`의 상수와 일치함을 확인(표준 NUS와 반대인 것도 재확인)
  - `MOCK_HARDWARE=false`로 서비스 기동 → `/health`가 `microbit_connected: true` 반환
  - `/command` 7종(G1~G7) 전송 + AI Hand 손모양 육안 대조 완료 — 엄지 미동작으로
    `G3`↔`G6`, `G4`↔`G7`이 구별되지 않음을 확인(아래 "손모양 육안 대조 결과" 참고)
  - `/result` correct/incorrect → LED O/X 표시 정상 확인 (아래 부저 수정 후)
  - `/progress` → `P37`/`P17` 전송에 `OKP37`/`OKP17` 회신 확인(`status: ok`). LED 표시가 없는
    명령이라 회신값이 유일한 성공 근거인데, 왕복까지 정상으로 확인됨
- 검증 중 발견한 환경 이슈 2건(문서화 목적):
  - PowerShell에서는 bash식 `MOCK_HARDWARE=false uvicorn ...`이 안 된다 → `$env:MOCK_HARDWARE = "false"` 후 실행
  - PowerShell의 `curl`은 `Invoke-WebRequest` 별칭이라 `-d '{"..."}'`의 큰따옴표가 벗겨져 JSON이 깨진다
    → `curl.exe`를 쓰거나 `Invoke-RestMethod ... -Body (@{...} | ConvertTo-Json)` 사용.
    한글 `target_signal`은 파일(`cmd.json`, UTF-8)로 만들어 `--data-binary @cmd.json`으로 보낼 것

**이슈/블로커**
- 🐛 **패닉 070 재발** — `/result` 테스트 중 **부저가 울리는 순간** micro:bit에 슬픈 얼굴 + `070`이
  표시되며 BLE가 끊겼다. 원인은 `aihand_control.ts`의 `playResultTone()`이 쓰는 `music.playTone()`이
  micro:bit v2 BLE SoftDevice와 충돌하는 것(SD_ASSERT).
- 이것은 **이미 알려진 문제의 회귀**였다 — `StartbitV2_patched.ts:324`에 "원래는 music.playTone() -
  BLE SoftDevice 충돌(070 panic) 방지를 위해 소리 대신 LED로 대체"라는 주석이 남아 있는데, 나중에
  RESULT 기능을 추가하면서 같은 호출을 다시 넣었던 것이다.

**결정 사항**
- **부저는 사용하지 않는다(확정).** `aihand_control.ts`에 `const BUZZER_ENABLED = false;`를 추가하고
  `playResultTone()` 최상단에서 가드한다. 부저가 꺼져 있을 때는 `showResult()`가 `basic.pause(2000)`으로
  **LED 표시 시간 2초를 그대로 유지**한다(기존엔 부저 1초 블로킹 + 1초 대기로 2초였음).
  코드를 삭제하지 않고 플래그로 남긴 것은, 패닉 없이 소리 내는 방법을 찾으면 한 줄로 되살리기 위함이다.
- 판정 피드백은 **LED O/X만** 사용 — `02_설계문서_v2`, `03_인터페이스계약서_v2`,
  `shared/schemas/microbit_protocol.md`, `services/actuation/README.md`를 모두 이 기준으로 갱신했다.
- 재플래시 후 정상 동작(소리 없이 O/X 표시, 패닉·BLE 끊김 없음) 확인 완료.

**A-1 검증 결과 요약**

| 항목 | 결과 |
|---|---|
| BLE 스캔·연결 (`/health`) | ✅ `microbit_connected: true` |
| UUID 실측 대조 | ✅ 코드 상수와 일치(RX/TX 반전 재확인) |
| `POST /command` 7종 | ✅ 전송·응답 확인 + 손모양 육안 대조 완료 (아래 참고) |
| `POST /result` | ✅ LED O/X 표시, 패닉 없음 (부저 비활성화 후) |
| `POST /progress` | ✅ `OKP37`/`OKP17` 회신 확인 |
| RPi5 컨테이너 BLE 접근 | ⬜ 미실시 (RPi5 실물 필요) |

**손모양 육안 대조 결과 (2026-09-21 실측)**

- 엄지 미동작 상태에서 **`G3`(좌회전_유도)↔`G6`(후진), `G4`(우회전_유도)↔`G7`(주의)이 실제로
  구별되지 않음을 육안으로 확인**했다. 예상대로였고, 이제 추정이 아니라 실측 확정 사항이다.
- 영향: AI Hand **시범 단계**에서 7종 중 4종(G3/G4/G6/G7)이 2쌍으로 겹친다. 학습자가 로봇손만
  보고는 무엇을 따라 해야 할지 판단할 수 없다.
- 단, 교육 흐름 자체가 막히지는 않는다 — web 화면이 현재 목표 수신호 이름(`target_signal`)을
  이미 표시하므로(`GET /api/state`), 학습자는 **화면의 이름으로 구분하고 AI Hand는 보조 시범**으로
  기능한다. 인식·판정 경로(vision → 판정)는 로봇손과 무관하므로 KPI에는 영향이 없다.
- 제스처 재설계는 하지 않기로 이미 확정(위 결정 사항) — 시현 대응은 web 화면 안내에 의존한다.
  화면 문구/이미지 보강 필요 여부는 조은수(web 담당)와 협의 대상.

**다음 할 일**
- [ ] RPi5 실물에서 컨테이너 BLE(BlueZ/D-Bus) 접근 검증
- [ ] (협의) 시범 단계에서 겹치는 4종을 화면으로 어떻게 구분시킬지 — web 담당과

### 2026-09-21 (추가) — B-1 picar 모터 I2C 프로토콜 확보 및 구현

**작업 내용**
- **Raspbot 모터 I2C 명령 프로토콜 확보** — Yahboom 공식 드라이버 라이브러리 `YB_Pcb_Car.py`에서
  레지스터/바이트 포맷을 확인했다. 그동안 "벤더 문서가 없어 구현 불가"로 막혀 있던 항목이다.

  | 레지스터 | 용도 | 바이트 포맷 |
  | --- | --- | --- |
  | `0x01` | 모터 구동 | `[좌_방향, 좌_속도, 우_방향, 우_속도]` (방향 0=후진/1=전진, 속도 0~255) |
  | `0x02` | 정지 | 단일 바이트 `0x00` |
  | `0x03` | 서보 | `[서보ID, 각도(0~180)]` — 카메라 팬/틸트용, 주행 미사용 |

  슬레이브 주소는 **`0x16`**(Raspbot 오리지널). ⚠️ Raspbot **V2는 `0x2B`** 라 보드가 다르면 값이
  달라진다 — 실물에서 `i2cdetect -y 1`로 확인하고 `RASPBOT_I2C_ADDR`로 덮어쓸 수 있게 해뒀다.
  우리 보드를 오리지널로 추정한 근거: `hardware_pinmap.md`에서 초음파·부저·LED·트래킹이 전부 Pi
  GPIO 직결인데, V2는 이 주변장치들까지 MCU가 I2C로 관장한다.
- `services/picar/src/controller.py` 실물 구동 구현 — 기존 `NotImplementedError` 제거
  - 모터: `stop`/`forward`/`backward`/`left`/`right` → I2C 레지스터 쓰기. 스키마의 speed(0~100%)를
    코프로세서용 0~255로 변환
  - 좌/우 회전은 **제자리 회전**(한쪽 전진 + 반대쪽 후진)으로 구현 — 교육 시범이라 회전이 눈에
    확실히 보이는 편이 낫다는 판단(완만한 선회로 바꾸려면 한쪽 속도를 0으로)
  - LED: `gpiozero`로 적색(BCM21) on/off/blink 구현. 황색 2개는 미배선이라 `unsupported` 응답
  - I2C 실패해도 예외를 삼키고 응답에 담아 돌려준다 — web이 picar 실패로 학습 흐름을 막지 않는
    정책과 맞춤(03_인터페이스계약서_v2 §7)
  - `MOCK_HARDWARE=true`에서 **실제 전송될 주소·레지스터·바이트 배열을 응답에 담도록** 했다.
    보드가 없어도 프로토콜 변환이 맞는지 미리 검토할 수 있다 — mock 4종으로 확인 완료
    (예: 서행 speed=40 → `addr 0x16, reg 0x01, data [1, 102, 1, 102]`)
- `services/picar/requirements.txt`에 `smbus2==0.5.0` 추가 (실물 경로에서만 지연 임포트)
- `services/picar/README.md`에 프로토콜 표와 **실물 검증 절차**(i2cdetect → 차를 들어 올린 채 저속
  전진 → 자동 정지·회전 방향·LED 확인) 추가
- 결정 요청 문서 작성 — `document/picar_주행시간_스키마_변경안.md` (주행 지속 시간 / 물리 피드백
  지연 KPI 정의 / 확인_완료 LED 패턴 3건). `shared/schemas/` 변경은 전원 리뷰 대상이라(README §4)
  코드를 바꾸지 않고 제안서로 올림
- PR 본문 준비 — `document/PR_본문_picar_I2C_구현.md` (`feature/picar` → `dev`)
- **산출물 갱신**
  - `08_리스크레지스터.md`: 신규 3건 등록(무한 주행 / 물리 피드백 지연 KPI 달성 불가 / I2C 주소
    미검증) + **옛 정보 3건 정정** — ① micro:bit 통신이 "USB 시리얼 고정, 무선 미사용"으로 남아
    있던 것을 BLE 전환 및 실물 검증 완료로 ② picar 제어가 "RPi4B GPIO 직결"로 잘못 적혀 있던 것을
    I2C 코프로세서 경유로 ③ 엄지 서보를 "교체 필요/대응 중"에서 "교체 안 함 확정"으로
  - `03_인터페이스계약서_v2.md` §5-2: picar 모터 I2C 프로토콜 표 추가, 주행 지속 시간 필드 부재를
    🔴 미결로 명시, 변경 이력에 항목 추가

**이슈/블로커**
- ⚠️ **주행 지속 시간이 스키마에 없다.** `picar_command.schema.json`에 `duration` 계열 필드가 없어서
  명령을 그대로 흘리면 다음 명령이 올 때까지 차가 계속 달린다(벽으로 돌진). 임시로 주행 명령 후
  `MOTION_DURATION_S`(기본 2초) 뒤 자동 정지하는 안전장치를 넣었다 — **잠정값이며 팀 확정 필요**.
  스키마에 `duration_ms`를 추가할지도 함께 결정해야 한다.
- 위 구현은 전부 **실물 미검증**이다 (RPi4B·picar 미도착). 특히 슬레이브 주소 `0x16` 가정과
  좌/우 배선 방향은 실물에서 반드시 확인해야 한다.
- `확인_완료`의 "적색·황색 번갈아 2회 점멸 후 소등"은 현재 스키마(LED별 on/off/blink)로는 표현할 수
  없다 — web이 red=blink, yellow=blink로 보내고 있어 "번갈아"가 아니라 "동시 점멸"이 된다.
  시퀀스 표현이 필요하면 스키마 확장이 필요하다(미결).

**결정 사항**
- 슬레이브 주소를 상수 하드코딩이 아니라 `RASPBOT_I2C_ADDR` 환경변수로 덮어쓸 수 있게 했다 —
  오리지널/V2 판별이 실물 전에는 불가능하기 때문.
- 주행은 무한 지속시키지 않고 **자동 정지를 기본 동작으로** 한다(안전 우선). 시간 값만 팀이 조정.

**다음 할 일**
- [ ] `feature/picar` 푸시 → PR 생성(base: `dev`) → 팀 리뷰로 결정 3건 수령
- [ ] RPi4B 도착 후 `i2cdetect -y 1`로 슬레이브 주소 확인 → 필요시 `RASPBOT_I2C_ADDR` 조정
- [ ] 차를 들어 올린 상태에서 저속 전진/후진/좌회전/우회전 + 자동 정지 검증
- [ ] 주행 지속 시간 확정 (스키마 `duration_ms` 추가 여부 포함) — 팀 협의
- [ ] 황색 LED 2개 배선 + BCM 핀 확정 후 `PIN_MAP` 채우기

### 2026-09-21 (추가) — picar·actuation 단위 테스트 도입

**작업 내용**
- `services/picar/tests/test_controller.py` **18개**, `services/actuation/tests/test_controller.py`
  **14개** 작성. 둘 다 **하드웨어 없이** 돌아간다(mock 경로만 사용).
  - 그동안 vision만 테스트가 있었고(19개) picar·actuation은 0개였다. 특히 picar는 B-1에서 검증되지
    않은 로직 215줄이 한 번에 들어간 상태였다.
  - 목적은 실물이 도착했을 때 **"코드가 틀린 건지 배선이 틀린 건지"** 로 헤매지 않는 것이다.
    W4 일정에 그 디버깅을 하고 싶지 않다.
- picar 측 커버: 속도 변환(0~100%→0~255, 경계·클램프·web 실제값), action별 레지스터/바이트,
  **좌/우 거울상 여부**, 자동 정지 예약, 미배선 LED의 `unsupported` 응답, `PIN_MAP`↔`USED_BCM_PINS`
  충돌 검사, mock에서 `smbus2`/`gpiozero` 미임포트
- actuation 측 커버: `GESTURE_MAP`↔스키마 7종 일치, G번호 1~7·PRD 표 순서, BLE 문자열 형식
  (`G{n}`/`correct`/`incorrect`/`P37`), **RX/TX UUID 반대 배정 고정**, 이상 입력 방어,
  mock에서 BLE 미연결
- 스키마 파일(`shared/schemas/*.json`)을 테스트가 직접 읽어 비교하도록 했다 — 계약이 바뀌면
  테스트가 먼저 깨져서 알려준다
- `pytest-asyncio`를 추가하지 않으려고 async 호출은 `asyncio.run()`으로 감쌌다 (의존성 증가 회피)

**검증**
- picar 18 / actuation 14 / vision 19 = **전체 51개 통과**
- 테스트가 실제로 버그를 잡는지 **변이 검사**로 확인: `controller.py`의 좌/우 분기를 일부러
  뒤바꿨더니 `test_left_and_right_are_mirrored_spin_turns`가 정확히 실패했고, 되돌린 뒤 다시 통과.
  (`git diff`로 원상복구 확인 완료)

**부수 효과**
- 황색 LED 핀을 `PIN_MAP`에 채우는 순간 `USED_BCM_PINS`와의 충돌이 자동 검사된다 — 배선 전에
  핀 선택 실수를 잡을 수 있다
- 06_테스트기록_v2(W4 산출물)의 "단위 테스트" 근거 자료로 그대로 쓸 수 있다

**다음 할 일**
- [ ] 실물 도착 후, 이 테스트가 통과하는 상태에서 배선만 의심하면 되는지 확인
- [ ] web 상태머신(`state_machine.py`)도 테스트 0개 — 담당(조은수)에게 공유

### 2026-09-21 (추가) — picar LED 배선 확정

**배경 — 문서마다 LED 개수가 달랐다**

| 출처 | LED 구성 |
| --- | --- |
| 02_설계문서 §3-2 · 10_PRD §6 · 시스템_구성도 | 적색×2 + 황색×2 = **4개** |
| `picar_command.schema.json` | `red`/`yellow_left`/`yellow_right` = **3채널** (적색이 1로 축약) |
| `controller.py` `PIN_MAP`(이전) | 내장 적색 1 + 황색 2(미배선) = **3개** |
| Raspbot 보드 실물 | 내장은 적색 1 + **청색** 1뿐 |

외부 배선 대상이 **황색 2개로만** 잡혀 있어서, 이대로 배선했으면 "정지 → 양쪽 적색 점등"이
**한쪽만 켜지는** 상태가 될 뻔했다. 적색 2번째를 아무도 챙기지 않고 있었다.

**결정 사항**
- 수신호 7종을 전수 확인한 결과 **적색이 한쪽만 켜지는 경우가 없다**(정지=양쪽 점등,
  확인_완료=양쪽 점멸). 따라서 적색은 개별 제어가 불필요하고, 스키마가 `red` 단일 채널인 것도 타당.
- **외부 LED 4개를 GPIO 3핀으로 구동하기로 확정** — 적색 2개를 한 핀에 병렬 (⚠️ **2026-09-22에 2핀으로 분리됨** — 아래 로그 참고):

  | 채널 | BCM | 물리 핀 | 외부 LED |
  | --- | --- | --- | --- |
  | `led_red` | 13 | 33 | 적색 2개(병렬) |
  | `led_yellow_left` | 5 | 29 | 황색 1개 |
  | `led_yellow_right` | 6 | 31 | 황색 1개 |

- **내장 적색(BCM21)은 쓰지 않는다.** 내장 LED의 직렬저항 값을 모르는 상태에서 그 핀에 외부 2개를
  더 물리면 핀 전류 한도(기본 8mA/최대 16mA)를 넘길 수 있다. 부하가 예측 가능한 여유 핀을 쓴다.
- 저항은 LED마다 개별로(공통 저항 금지 — 밝기 불균일·전류 쏠림). 적색 Vf≈2.0V 기준 330Ω이면
  개당 약 3.9mA, 2개 합쳐 약 7.9mA로 기본 구동 한도 안.

**작업 내용**
- `controller.py`의 `PIN_MAP`을 확정 핀으로 채우고, docstring "LED" 절에 배선표·근거·전류 계산 기록
- `services/picar/README.md`에 "LED 배선" 절 신설, "아직 확정 안 된 것"의 황색 LED 항목 완료 처리
- `02_설계문서_v2.md` §3-2의 "적색 2개(좌/우)"에 **제어 채널은 1개**라는 점과 확정 핀을 명시
  (좌/우 개별 제어가 필요한 건 황색뿐)
- 단위 테스트 갱신: 미배선 전제 테스트를 제거하고 **핀 배정 완료·핀 중복 없음·Raspbot 점유 핀 미충돌·
  스키마 enum과 상태값 일치**를 검사하도록 교체 (picar 18 → **21개**)

**🐛 테스트가 잡은 결함 1건**
- `_apply_led()`가 **mock 분기에서 상태값 검증을 건너뛰고 있었다.** `_apply_motor()`는 action을
  먼저 검증하는데 LED만 순서가 달라서, mock 모드로는 잘못된 상태값(`"strobe"` 등)을 걸러내지
  못했다 — 보드 없이 하는 테스트의 의미가 반감되는 문제.
- 검증을 mock 분기보다 앞으로 옮기고, 허용 상태를 `LED_STATES` 상수로 뽑아 스키마 enum과 대조하는
  테스트를 추가했다.

**다음 할 일**
- [ ] 실물 배선: 적색 2개 → BCM13(물리 33) 병렬, 황색 좌 → BCM5(29), 황색 우 → BCM6(31),
  각 LED에 330Ω 개별 직렬저항
- [ ] 배선 후 `MOCK_HARDWARE=false`로 3채널 on/off/blink 확인

### 2026-09-21 (추가) — 하드웨어 설계서 초안 작성

**작업 내용**
- `document/11_하드웨어설계서_v1.md` 신규 작성. 그동안 `hardware_spec.md`(부품 원본 스펙),
  `hardware_pinmap.md`(핀맵), 각 서비스 README, `03_인터페이스계약서` §5, `네트워크_구성_결정.md`,
  `시스템_구성도_초안.md`에 흩어져 있던 하드웨어 정보를 한 문서로 통합했다.
- 단순 취합이 아니라 **"왜 이렇게 설계했는가"의 근거를 실측값과 함께** 남기는 데 중점을 뒀다.
  실물을 처음 만지는 사람이 이 문서 하나로 배선하고 검증할 수 있는 것이 목표.
- 구성: ①구성 개요(보드 2대 분리 근거) ②컴퓨트 보드 ③카메라 ④AI Hand+micro:bit ⑤picar
  ⑥네트워크 ⑦전원 ⑧설계 제약이 KPI에 미치는 영향 ⑨실물 검증 체크리스트 ⑩미확정 항목 ⑪변경 이력
- 항목마다 ✅확정(실측) / 🟡확정(미검증) / ⬜미확정 을 표기해, 어디까지가 검증된 사실인지 구분했다.

**작성 중 드러난 것**
- **전원 여유 계산을 처음으로 정리했다** — 손가락 5개 동시 구속 시 3.5A, 손목까지 더하면 약 6.0A로
  AI Hand의 7.5V 3A 어댑터를 크게 넘는다. 즉 순차 구동 규칙은 micro:bit 보드 공급 한계(300mA)뿐
  아니라 **어댑터 용량 관점에서도 필요**했다. 그동안은 보드 한계만 근거로 들고 있었다.
- ⬜ **새 미확정 항목 발견 — 서보 전원 경로.** "micro:bit 보드 공급 300mA" 논거는 서보가 micro:bit
  에서 급전된다고 가정한 것인데, AI Hand는 별도로 7.5V 3A 어댑터를 쓴다. 실제 급전 경로(micro:bit
  3V 레일 / 확장보드 레귤레이터 / 어댑터 직결)를 명시한 문서가 없다. 어느 쪽이든 순차 구동이
  대응책인 건 같지만, **외부 전원 보강으로 동시 구동이 가능해지는지** 판단하려면 확정이 필요하다.
- 하드웨어 제약과 KPI의 충돌을 §8에 표로 정리 — 200ms 순차 이동(~1.0초)과 LED 2초 표시는 둘 다
  줄일 수 없어서, "물리 피드백 지연 P95≤2.0초"를 "완료" 기준으로 읽으면 달성 불가라는 점을
  하드웨어 문서에서도 명시했다.
- `10_PRD_v2.md` §12 참고 문서 색인에 등록

**다음 할 일**
- [ ] 서보 전원 경로 확인 (실물 측정)
- [ ] 실물 검증 결과와 팀 결정 3건이 나오면 v2로 개정

### 2026-09-21 (추가) — ⚠️ 전원 구조 근거 정정 (기존 문서 6곳 오류)

**무엇이 틀렸나**

그동안 "손가락 서보 동시 구동 금지"의 근거로 여러 문서가 이렇게 적고 있었다:

> micro:bit v2 보드 최대 공급 전류 약 300mA < 손가락 서보(LFD-01) 구속 전류 700mA
> → 서보 하나만 걸려도 보드 공급 한계를 넘는다

**이 설명은 틀렸다.** 실제 전원 구조를 확인한 결과(2026-09-21):

```
[7.5V 3A 어댑터] ──▶ [Hiwonder micro:bit 확장보드] ──┬──▶ micro:bit v2
                            (전원 분배)              ├──▶ 서보 핀 6개 (각 핀 7.5V)
                                                    ├──▶ 각종 입출력 인터페이스
                                                    └──▶ DC 모터 전용 핀
```

**서보는 micro:bit를 거치지 않는다.** 확장보드가 어댑터에서 직접 분배한다. micro:bit의 300mA는
micro:bit가 *외부로* 공급할 수 있는 한계치라서 서보 전류와는 애초에 상관이 없는 수치였다.

**실제 메커니즘**: 어댑터 1개가 확장보드를 거쳐 **micro:bit와 서보를 같은 레일에서** 먹인다.
손가락 서보 5개를 동시 기동하면 3.5A(구속 기준)로 어댑터 용량 3A를 넘겨 7.5V 레일이 주저앉고,
**같은 레일의 micro:bit가 브라운아웃되어** BLE SoftDevice가 죽는다(패닉 070). 순차 구동은 이
돌입 전류를 시간축으로 흩어 놓는 방식이다.

**결론은 안 바뀌지만 함의가 바뀐다**: 순차 구동이 필요한 건 동일하다. 다만 기존(잘못된) 모델에서는
어떤 수를 써도 동시 구동이 불가능했는데, 실제 제약이 어댑터 용량이라면 **전원 보강(고용량 어댑터 +
서보 레일 벌크 커패시터)으로 동시 구동이 가능해질 여지가 있다.** 일정상 후순위로 두고 기록만 남긴다.

**🔴 정정 과정에서 새로 발견한 위험 — 서보 정격 전압 초과 가능성**

확장보드의 서보 핀은 **7.5V를 그대로 내보내는데**, LFD-01과 LD-1501MG의 정격 작동 전압은 둘 다
**DC 4.8~6.0V**다(`hardware_spec.md`). 사실이라면 정격을 25% 초과해 구동 중이다.

- **엄지 서보 고장의 유력한 원인**일 수 있고, 그렇다면 **나머지 4개도 같은 위험**에 노출돼 있다 —
  시연 전에 추가 고장이 날 수 있다.
- 다만 Hiwonder가 이 서보들과 7.5V 어댑터를 한 제품으로 함께 출하하므로, 의도된 오버볼트이거나
  확장보드가 서보 핀에서 강압할 가능성도 있다.
- → **멀티미터로 서보 핀 실제 전압을 측정**해야 확정된다. 6V 이하면 문제없고, 7.5V가 그대로
  나오면 여분 서보 확보 또는 강압 검토가 필요하다.

**정정한 문서 6곳 + 신규 1곳**
- `11_하드웨어설계서_v1.md` §4.4·§7 — 전원 경로 확정, 메커니즘 재작성, 정격 초과 위험 추가
- `03_인터페이스계약서_v2.md` §5-3
- `services/actuation/README.md`
- `services/actuation/src/microbit/ble_bridge.py` (docstring)
- `shared/schemas/microbit_protocol.md`
- `picar_주행시간_스키마_변경안.md` (KPI 근거 문구)
- `08_리스크레지스터.md` — 기존 행 근거 정정 + **신규 리스크 "서보 정격 전압 초과 가능성"** 등록

**실측 결과 (2026-09-21, 같은 날 확인)**
- 확장보드 서보 핀 전압 **7.46V**로 측정됨 (안정적으로 유지). **강압 없음 — 어댑터 전압이 그대로
  서보에 걸린다.** LFD-01·LD-1501MG 정격이 DC 4.8~6.0V이므로 **정격 상한의 약 124%**로 확정.
- 전류 계산도 보정했다. 구속된 DC 모터 전류는 전압에 거의 비례하므로(I ≈ V/R_winding), 데이터시트
  6V 기준값에 1.24배를 적용하면 손가락 1개 약 **870mA**, 5개 동시 약 **4.35A**, 손목까지 더하면
  약 **7.5A**다 — 어댑터 3A 대비 초과 폭이 데이터시트 기준보다 더 크다. 순차 구동의 필요성이
  오히려 강화됐다.
- **엄지 서보 고장의 유력한 원인**이며, 나머지 손가락 서보 4개도 같은 조건이다. 다만 Hiwonder가
  이 서보와 7.5V 어댑터를 한 제품으로 출하하므로 **제조사가 의도한 오버볼트**로 보인다(토크 확보
  목적 추정). 수명 단축은 감수하는 구조.

**대응 방침 (결정)**
1. **여분 LFD-01 확보** ← 1순위. 저가 부품이고, 시연 중 고장에 대한 유일한 실질적 보험.
2. 불필요한 서보 구동 최소화 — 반복 내구 테스트 지양, 리허설도 필요한 만큼만.
3. **강압/저전압 어댑터 교체는 하지 않는다.** 확장보드의 입력 전압 허용 범위와 내부 레귤레이터
   구성을 모르는 상태에서 손대면 보드 전체를 잃을 수 있고, 남은 일정(W4 ~10/04)에 감당할 위험이
   아니다. 시도한다면 확장보드 입력 전압 사양 확인이 선결.
- 참고: 가동범위 clamp가 이미 펌웨어에 적용돼 있어 구속(stall) 상태는 방어되고 있다 —
  오버볼트 피해가 가장 커지는 조건을 피하는 셈이라, 이 부분은 다행히 이미 대비되어 있었다.

**다음 할 일**
- [ ] 여분 LFD-01 서보 구매

### 2026-09-21 (추가) — RPi5↔picar 통신 신뢰성 + IP 구성 확정

**IP 구성 확정 — 공유기 없이 1:1 무선 직결**
- RPi5의 Wi-Fi를 **AP 모드**로 띄우고 picar(RPi4B)가 접속하는 구조, **양쪽 고정 IP**로 확정.
  RPi5 `192.168.50.1/24`, picar `192.168.50.10/24`. PC 직결(UTP)은 `192.168.10.x`로 서브넷이 달라
  두 링크가 섞이지 않는다.
- **공유기를 안 쓰는 이유**: 발표장 공용 Wi-Fi 품질은 통제 밖 변수다. 다른 팀 기기 간섭으로
  picar의 500ms 예산이 잠식되면 그 자리에서 할 수 있는 게 없다. `네트워크_구성_결정.md`에서
  RPi5↔PC를 UTP 직결로 정한 것과 같은 원칙 — **시연 경로에서 외부 인프라 의존을 전부 제거**.
- **mDNS 대신 고정 IP**: 이름 해석이 한 단계 더 끼어들고 Avahi 상태에 따라 간헐 실패한다.
  장치가 2대뿐이라 이름을 쓸 이득이 없다.
- ⚠️ **트레이드오프**: RPi5는 무선 IF가 1개뿐이라 AP 모드에서 스스로 인터넷에 못 나간다(이더넷은
  PC 직결에 사용). → 개발 중엔 공유기에 붙여 쓰고 **시연 직전에 AP로 전환**한다.
- `11_하드웨어설계서_v1.md` §6.1에 IP 표·근거·`nmcli` 구축 절차 기재. `시스템_구성도_초안.md` §5의
  미확정 체크 해소, `네트워크_구성_결정.md`에 상호 참조 추가.

**picar `/health` 실질화**
- 기존에는 프로세스만 살아 있으면 무조건 `ok`였다 — **"I2C가 죽었는데 HTTP 200"** 을 구분할 수 없었다.
- `controller.diagnose()` 추가: I2C 코프로세서 ACK 여부, LED 핀 배정, 주행 중 여부를 보고한다.
  I2C 프로브는 `write_quick`(주소만 전송)이라 **모터를 돌리지 않는 비파괴 검사**다.
- `/health`가 `status: "ok" | "degraded"`를 돌려주도록 변경 — 호출부가 한 필드만 봐도 된다.
- 단위 테스트 5개 추가 (picar 21 → **26개**)

**web 쪽 개선은 변경안으로 전달**
- `services/web/backend/state_machine.py`는 **조은수 담당**이라 직접 수정하지 않고
  `document/web_picar_통신_신뢰성_개선안.md`로 정리했다.
- 핵심 발견: `_post_with_retry`가 반환값을 버리는 데다, **`httpx.post()`는 4xx/5xx에 예외를 던지지
  않는다.** 즉 지금 구조는 **서버 오류(HTTP 500)까지 성공으로 세고 있다.** picar가
  `{"status":"partial","motor":{"reason":"i2c_failed"}}`를 반환해도 web은 정상으로 인식한다 —
  시연 중 모터가 안 돌아도 화면은 멀쩡해 보인다.
- 제안: ①`_post_with_retry` 결과 반환(+4xx/5xx 처리) ②전송 결과를 `/api/state`에 노출
  ③`/health` 주기 확인 ④폴링 블로킹 완화(A/B/C 택1, 나는 C 선호)
- `.env.example` 정리: 실물 고정 IP 예시 추가, 옛 `MICROBIT_PORT=/dev/ttyACM0`(USB 시리얼 시절
  유물)을 `MICROBIT_BLE_NAME`으로 교체

**다음 할 일**
- [ ] 개선안을 조은수에게 전달하고 ④번(폴링 블로킹) 방식 합의
- [ ] 실물 보드 2대 확보 후 AP 모드 구축 및 왕복 지연 측정
- [ ] RPi5 Wi-Fi 칩이 AP 모드에서 5GHz를 지원하는지 확인 (2.4GHz는 발표장 혼잡 우려)

### 2026-09-22 — 제작사 이미지 분석 · 1차 구현 임시 구성 · 구성도 v4

**① 제작사 제공 SD 이미지 분석** (`11_하드웨어설계서_v1.md` §5.5)

Yahboom이 제공한 RPi4B 전용 이미지 `Raspbot_rpi4B.img`(15.6GB)의 파티션을 추출해 내부를 확인했다.
지금까지 문서에 적어둔 I2C 사양이 **추정에 가까웠는데, 이번에 근거가 생겼다.**

- **§5.2 I2C 프로토콜 확증**: 이미지 안의 `YB_Pcb_Car.py` **17개 사본이 전부 md5 동일**하고,
  슬레이브 주소 `0x16`·레지스터 `0x01`(모터)/`0x02`(정지)/`0x03`(서보) 구성이 우리 문서 표와
  정확히 일치했다. 제품 식별자도 `Raspbot_Pi_V1.0`으로 확인됐다.
  → **`services/picar/src/controller.py`는 수정할 것이 없다.** (최종 확인은 여전히 실물 `i2cdetect -y 1`)
- 🔴 **핀 번호 체계 함정 발견**: 제작사 코드는 전부 `GPIO.setmode(GPIO.BOARD)`(물리 핀 번호)를
  쓴다. 우리는 BCM 기준이므로 **제작사 예제 상수를 그대로 옮기면 전혀 다른 핀을 잡는다.**
  13핀 대조표를 §5.5.3에 남겼다 — 기존 §5.4 점유 핀표와는 일치했다.
- **제작사 코드 결함 4건** 기록(§5.5.5): 서보 각도 클램프가 리스트 생성 *뒤*에 있어 무효, 펄스 변수
  windup, GPIO9/10 ↔ SPI 충돌, IR 핀 이중 점유. **참고는 하되 이식하면 안 되는 지점**이다.
- **재사용 판단**: 가져올 것은 **I2C 프로토콜 사양뿐**. 이미지 전체는 Buster(EOL) + TF 1.14 +
  `force_turbo=1` + SSH/VNC 기본 활성이라 그대로 쓸 수 없고, Flask·WebSocket 스택은 우리 아키텍처와
  역할이 겹친다. **자체 OS에 `services/picar`만 올리는 기존 방침이 타당함을 확인**했다.
  단, 자체 OS 설치 시 `dtparam=i2c_arm=on`을 직접 켜야 한다(기본 비활성) — 체크리스트에 추가.
- 부수 확인: 제작사 기본 속도 `car_speed=100`(0~255), 완만한 선회는 한쪽 절반 속도, 제자리 회전은
  방향 비트 반전 — 우리 구현(좌/우를 제자리 회전으로 처리)과 같은 방식이다.

**② 1차 구현 임시 구성 결정 — RPi5를 picar에, PC를 스테이션에** (`11_하드웨어설계서_v1.md` §1.4)

**RPi4B(8GB) 미도착으로 picar 실물 작업이 막혀 있었는데**, Raspbot 차체가 RPi5도 지원한다는 점을
이용해 역할을 임시로 바꿔 진행하기로 했다. RPi5를 picar에 올리고, 원래 RPi5가 맡을 고정 스테이션은
**PC가 대행**한다.

- **코드 수정이 필요 없다는 것을 먼저 확인했다** (§1.4.3). RPi5는 RP1 칩 때문에 `RPi.GPIO`가
  동작하지 않는데, `services/picar`는 **`gpiozero` 2.0.1**을 쓰고 있어 lgpio 백엔드로 그대로 붙는다.
  `bleak`도 크로스플랫폼이라 Windows BLE 스택에서 동작한다. **운 좋게 맞은 게 아니라, 처음부터
  `gpiozero`를 고른 판단이 여기서 이득을 봤다.**
- 🔴 **제약**: Windows Docker Desktop이 USB 웹캠·호스트 BLE를 전달하지 못한다. **vision·actuation은
  PC에서 네이티브 실행**해야 하고, web·signdb만 Docker로 띄운다.
- ✅ **부수 효과 — 카메라 미도착이 더 이상 vision의 블로커가 아니다.** `capture.py`의 picamera2
  경로는 `NotImplementedError` 상태지만, `scripts/webcam_check.py`·`record_dataset.py`에는
  `cv2.VideoCapture` + Windows `CAP_DSHOW` 분기가 **이미 구현돼 있다.** USB 웹캠으로 진행 가능하다.
- 🟡 **미확정 4건**: RPi5의 Raspbot 배터리 구동 가능 여부(RPi5는 5V/5A 권장 — **차에 태우기 전에
  확인할 것**), Wi-Fi 구성(RPi5를 AP로 두면 §6.1 설계 재사용 가능하나 미확정), RPi5 방열,
  USB 웹캠 보유 여부.
- **한시적 구성이라 §2~§7 본문을 고치지 않고 §1.4로 분리**했다. RPi4B 도착 시 이 절만 걷어내면 된다.

**③ `시스템_구성도_초안.md` v3 → v4 갱신**

v3(2026-09-18) 이후 확정된 내용과 어긋난 채로 남아 있었다. **제어 경로 3건이 전부 실물과 달랐다.**

| v3 기술 (틀림) | v4 정정 |
| --- | --- |
| AI Hand 서보 "RPi5 직결" | micro:bit + StartbitV2 확장보드가 구동 |
| micro:bit "USB 시리얼" | **BLE (NUS)** |
| picar 모터 "GPIO로 직접 구동" | **I2C 코프로세서 `0x16`** 경유 |

- mermaid 다이어그램을 실제 체인(actuation → BLE → micro:bit → 확장보드 → 서보 / picar → I2C →
  MCU → 모터, GPIO → LED)으로 교체.
- **§2.1 제어 경로 요약표 신설** — 다이어그램만 보고 매체를 오해하는 것을 막기 위함. 위 3건은
  발표자료·옛 문서에도 퍼져 있을 수 있어 정정표를 §3에 명시적으로 남겼다.
- §5 체크리스트: IP 구성 해소 반영, 주행 시간·KPI 정의 미결정 2건을 명시적으로 추가.
- §6에 1차 구현 임시 구성 요약 추가, 물리 설계의 단일 출처가 `11_하드웨어설계서_v1.md`임을 명시.

**④ `.gitignore`에 `services/picar/reference/` 등록**

`Raspbot_rpi4B.img` 하나가 **15GB**다. GitHub는 파일당 100MB 제한이라 커밋 자체가 거부된다.
주석에 원본 출처(Yahboom 배포처)를 함께 남겨 나중에 다시 받을 수 있게 했다. 분석 결과는 전부
§5.5 문서와 `controller.py` 주석으로 옮겨져 있어 이미지가 없어도 참조에 지장이 없다.

**다음 할 일**
- [x] ~~RPi5를 Raspbot 배터리로 구동 → 전압 강하·재부팅 여부 확인~~ → **같은 날 ⑥에서 무효화**
  (전원 계통 분리 결정 — RPi5는 별도 어댑터)
- [ ] RPi5에 Bookworm 설치 + `dtparam=i2c_arm=on` → `i2cdetect -y 1`로 `0x16` 확인
- [ ] USB 웹캠 확보 → `webcam_check.py` 통과 후 `capture.py` 웹캠 경로 연결
- [ ] 1차 구현 Wi-Fi 구성(RPi5 AP ↔ PC) 확정

---

### 2026-09-22 (추가) — 부품 입수 · picar 전원 계통 분리 결정 · 1차 벤치 테스트 범위 확정

**⑤ 부품 3건 입수** (`11_하드웨어설계서_v1.md` §1.3)

| 부품 | 이전 | 현재 |
| --- | --- | --- |
| Raspbot 차체 (TT모터 4 + MCU) | ⬜ 미도착 | ✅ 보유 |
| 외부 LED (적색 2, 황색 2) + 330Ω 저항 4 | ⬜ 미구매 | ✅ 보유 |
| UTP 케이블 **cat.6** | ⬜ 미확인 | ✅ 보유 |

- 연동해서 §6 링크표(UTP 행 — 1차 구현에서는 미사용), §10 미확정 #9(구매 완료 처리)를 갱신했다.
- **여전히 막힌 것은 RPi4B(8GB)와 Pi Camera Module 3** 둘뿐이고, 이 둘은 §1.4 임시 구성으로
  우회 중이라 실물 작업을 시작할 수 있다.

**⑥ picar 전원 계통 분리 결정 🟡**

**모터·서보는 Raspbot 배터리, RPi5는 별도 어댑터**로 나누기로 했다.

- 부수 효과로 §7의 ⬜ 미확인 항목 *"모터 노이즈가 Wi-Fi 모듈에 영향을 주는지"* 가 **선제적으로
  해소된다.** 모터와 Pi가 레일을 공유하지 않으면 그 경로가 애초에 사라진다.
- **현재 어댑터는 5V/3A뿐이고 5V/5A는 배송 중.** 3A로 1차 테스트를 진행해도 되는지 검토한 결과
  **충분하다**고 판단했다:
  - 이번 구성에서 RPi5가 하는 일은 `services/picar`(FastAPI + I2C + LED 3핀)뿐이고 **USB 주변기기가
    하나도 붙지 않는다**(웹캠은 PC 쪽, 카메라 미사용).
  - RPi5 보드 단독 소비는 공식 자료 기준 유휴 약 3W / 고부하 약 9~12W(2.4A). 3A=15W면 여유가 있다.
    공식 27W PSU 요구는 **USB 주변기기 1.6A분 확보**가 목적인데 우리는 그것을 쓰지 않는다.
  - 예상 현상 2가지(동작 지장 없음): 부팅 시 *"5A를 공급할 수 없습니다"* 경고, USB 총 전류 600mA 제한.
  - ⚠️ 어댑터·케이블 품질은 확인할 것. 표기만 3A이고 CC 저항 구성이 부실하면 실제 1.5A로 떨어져
    언더볼티지(4.63V 미만)와 스로틀링이 발생한다. 의심되면 `vcgencmd get_throttled`.
- 🔴 **전류보다 배선이 위험하다 — 확인 2건** (블럭 위 테스트에서도 동일하게 적용):
  1. **확장보드의 헤더 5V 공급을 끊어야 한다.** Raspbot 확장보드는 통상 배터리 전원을 GPIO 헤더로
     Pi에 공급한다. 여기에 USB-C 어댑터를 동시에 꽂으면 두 전원이 같은 5V 레일에서 맞물리는데,
     **RPi5의 USB-C 입력에는 역류 방지가 있지만 헤더 5V 핀에는 없다.** 조립 전 확장보드의 5V 공급
     구조를 확인하고, 공급 구조라면 해당 라인을 차단한다.
  2. **GND는 반드시 공통 유지.** I2C(SDA/SCL)는 GND를 기준 전위로 삼는다. 5V만 끊고 GND는 연결
     상태를 유지해야 한다 — 분리하면 I2C가 아예 안 되거나 간헐적으로 깨진다.
- 🟡 **어댑터 방식은 주행 시연에 쓸 수 없다.** 벽 어댑터는 차를 케이블에 묶는다. 1차 벤치
  테스트(⑦)까지는 무관하지만, 실제 주행에는 배터리/PD 보조배터리가 필요하다. 다만 위 부하 계산대로면
  **PD 보조배터리 5V/3A로도 감당된다** — 5V에서 5A를 내는 보조배터리는 드물어 오히려 실용적이다.

**⑦ 1차 벤치 테스트 범위 확정 — 블럭 위, 바퀴와 LED만**

차체를 블럭 위에 올려 바퀴를 띄운 상태로 **구동 확인만** 진행한다. 주행·조향 튜닝은 이후로 미룬다.

- 이 범위에서는 어댑터 테더링이 문제되지 않으므로 **5V/3A로 그대로 진행 가능**하다.
- 기존 §9.2 체크리스트의 *"차를 들어 올린 상태에서 저속(speed=30) 전진"* 항목과 같은 방식이다.

**⑧ 테스트 절차 — 진단 경로를 활용한 3단계 분리**

`controller.py`에 이미 비파괴 진단이 들어 있어, 단계를 나누면 어디서 막혔는지 바로 드러난다.

| 단계 | 명령 | 확인 대상 | 바퀴 |
| --- | --- | --- | --- |
| 1 | `i2cdetect -y 1` → `GET /health` | I2C 코프로세서 ACK | **안 돎** |
| 2 | `POST /picar` (led만, `motor.action=stop`) | LED 3채널 배선 | 안 돎 |
| 3 | `POST /picar` (`forward`, `speed:30`) | 바퀴 회전 방향·자동 정지 | 돎 |

- 1단계가 **바퀴를 돌리지 않는 이유**: `_probe_i2c()`가 `write_quick`(주소만 전송, 데이터 없음)을
  쓴다. `/health`가 `degraded`면 주소가 V2(`0x2B`)일 수 있으므로 `RASPBOT_I2C_ADDR=0x2b`로 재시도.
- `speed`는 **퍼센트**다 — `_speed_pct_to_byte()`가 0~100을 0~255로 변환한다(30% ≈ 76).
  명령 1회마다 **2초 후 자동 정지**하므로 계속 돌지 않는다.
- 3단계에서 볼 것: `forward`인데 좌우 회전 방향이 다르면 배선 반전, `left`/`right`가 의도와 반대면
  좌우 교체(제자리 회전이라 눈에 띄지 않을 수 있음).
- 🔴 **2단계는 응답으로 판정할 수 없다 — 반드시 육안으로 확인할 것.** `PIN_MAP`의 3핀이 2026-09-21에
  전부 배정돼(`13`/`5`/`6`) `_apply_led`의 `unsupported` 경로는 **이제 발생하지 않는다**
  (`test_controller.py`가 `leds_unassigned == []`를 단언한다). 즉 **LED를 한 개도 결선하지 않아도
  `status: "ok"`가 돌아온다** — gpiozero는 핀을 잡는 데 성공했을 뿐, 그 끝에 LED가 달렸는지 알 방법이
  없다. 응답만 보고 "LED 배선 검증됨"으로 판단하면 **거짓 양성**이다.
- ⚠️ **정정(2026-09-22)**: 앞서 "`motor`를 빼면 422로 거부된다"고 적었으나 **틀렸다.**
  `POST /picar`는 FastAPI에서 `dict`로 받아 **스키마 검증을 하지 않고**, `execute()`가 `.get()`
  기본값으로 채운다 — `motor`가 없으면 `stop`으로 동작한다. 스키마상 필수인 것과 **서비스가
  강제하는 것이 다르다.** 벤치 테스트는 web이 실제로 보낼 형태(4개 필드)로 보내되, 이 차이는
  알고 있을 것.

**⑨ `11_하드웨어설계서_v1.md` 개정 누적** — v1.1(§5.5 이미지 분석) → v1.2(§1.3 부품 현황) →
v1.3(§1.4 1차 구현 임시 구성) → **v1.4(§7.1 전원 분리 + §9.2 벤치 테스트 절차)**.

**⑩ ⑥⑦⑧을 설계서에 반영 (v1.4)**
- **§7.1 신설** — 전원 계통 분리 확정, 5V/3A 충분 판단 근거표, 헤더 5V 백피드·GND 공통 확인 2건,
  주행 시연 전원 한계.
- **§7 계통표** — LED가 배터리가 아니라 **Pi의 GPIO 3.3V에서 급전**된다는 점 명시(적색 2개를
  BCM13 하나에 병렬 → 약 8mA로 핀당 16mA 한계 내), 1차 구현 행 추가.
- **§7 전류표 정정** — @6.0V 데이터시트 값만 남아 있어 §4.4(@7.46V 보정)와 어긋나 있었다.
  2026-09-21 실측 반영 때 §4.4만 고치고 §7을 빠뜨린 것으로, 두 열을 나란히 넣어 맞췄다.
- **§9.2 재구성** — 3단계 벤치 테스트 절차로 교체. 🔴 **2단계의 거짓 양성 경고 추가**:
  `PIN_MAP` 3핀이 모두 배정돼 `unsupported` 경로가 발생하지 않으므로 **LED를 결선하지 않아도
  `ok`가 돌아온다.** 응답이 아니라 육안으로 판정해야 한다(⑧의 최초 기술이 틀려 함께 정정).
- **§10** — #8 해소·#10 무효화, #13(헤더 5V 확인)·#14(주행용 PD 보조배터리) 신규. §1.4.6·§1.4.7의
  배터리 항목도 같이 무효화.
- **`시스템_구성도_초안.md` §5** — "picar 전원 계통 분리 여부" 체크 해소. 측정으로 판단할 문제가
  아니라 **구조로 없앤** 것이라는 점을 적어뒀다.

**⑪ 🐛 저장소 루트에서 `pytest`가 아예 돌지 않던 문제 수정**

⑩ 반영 후 회귀 확인차 전체 테스트를 돌리다 발견했다. `services/actuation/tests/test_controller.py`와
`services/picar/tests/test_controller.py`가 **파일명이 같아** 기본 import 모드(prepend)에서
`import file mismatch`로 **수집 단계에서 중단**된다 — 실패가 아니라 **아무 테스트도 실행되지 않는다.**

- 각각 따로 돌리면 통과하므로 지금까지 드러나지 않았다. 내가 2026-09-21에 두 파일을 만들면서
  같은 이름을 쓴 것이 원인이다.
- **루트에 `pytest.ini` 추가**하고 `--import-mode=importlib`로 해결했다. 파일 경로로 모듈을 적재해
  이름 충돌이 생기지 않는다. 파일명을 바꾸는 방법도 있지만, 앞으로 web/data가 같은 이름을 써도
  안전하도록 설정 쪽에서 막았다.
- 확인: 저장소 루트에서 `pytest` → **59개 전부 통과** (picar 26 · actuation 14 · vision 19).

**다음 할 일**
- [x] ~~§7 전원 설계·§10 미확정 항목에 ⑥ 전원 분리 결정 반영~~ → v1.4로 반영 완료 (⑩)
- [x] ~~조립 전 확장보드 헤더 5V 공급 구조 확인 → 차단~~ → **규칙 반전(2026-09-22)**: 단일 계통에서
  헤더 5V는 **급전 경로**다. 차단하지 않되 **USB-C 어댑터와 동시 사용 금지**
- [ ] 🔴 차체 강압 5V 정격 확인 + 벤치에서 `vcgencmd get_throttled` 실측 (언더볼티지/SD 손상 대비)
- [ ] RPi5에 Bookworm 설치 + `dtparam=i2c_arm=on` → `i2cdetect -y 1`로 `0x16` 확인
- [ ] 외부 LED 4개 배선(적색 2 병렬 → BCM13, 황색 → BCM5/6, 개별 330Ω)
- [ ] 블럭 위 3단계 테스트 수행
- [ ] 5V/5A 어댑터 수령 시 교체

### 2026-09-22 (추가) — micro:bit BLE ↔ `music` 병용 불가 확정, 규칙화

**확정된 결론**

부저 사용 코드를 직접 넣고 실행하는 전용 검증으로, 소리가 나야 할 지점에서 정확히 **070 패닉
(SD_ASSERT)** 이 발생함을 확인했다. 원인은 **BLE SoftDevice와 `music` 라이브러리가 같은 하드웨어
타이머/PWM 자원을 공유**하는 것으로, **micro:bit v2의 근본적 하드웨어 제약**이다.

- **이전까지의 인식과 다른 점**: 2026-09-21에는 "`music.playTone()`이 충돌한다"까지만 알고
  `BUZZER_ENABLED = false`로 **꺼두는** 조치를 했다. 그때 문서 4곳에 *"비블로킹 재생·볼륨 저감 등을
  검증하면 되살릴 수 있다"* 는 단서를 남겼는데, **이번에 그 가능성이 부정됐다.** 해당 문구를
  전부 철회했다.

**적용 규칙 (신규 기능 추가 시에도 유효)**
1. `bluetooth.startUartService()` 실행 이후에는 **`music.*` 함수를 절대 호출하지 않는다.**
2. 소리 피드백이 필요한 자리는 `basic.showIcon()`/`basic.showLeds()`/`basic.showString()` 등
   **LED 기반으로 대체**한다.

**🔴 코드 점검 — 규칙 준수 상태가 실제와 달랐다**

"기존 코드는 전부 `music` 호출 없음"으로 정리돼 있었으나, 실제로 확인하니 **`aihand_control.ts`에
`music.*` 호출이 4개(92~97행) 남아 있었다.** `BUZZER_ENABLED = false` 가드 뒤에 있어 런타임에는
실행되지 않지만, **소스에는 살아 있는 상태**였다.

- 참고로 노트에 있던 `aihand_production.ts`·`aihand_named_control.ts`·`aihand_finger_test.ts`는
  **현재 존재하지 않는다** — 2026-09-21에 `aihand_control.ts` 하나로 통합했다. 현 펌웨어 파일은
  `aihand_control.ts` / `StartbitV2_patched.ts` / `StartbitRGBLight.ts` 3개다.
- **조치: `playResultTone()`과 `BUZZER_ENABLED` 플래그를 삭제했다.** 되살릴 수 없는 코드를 플래그
  뒤에 남겨두면 **한 줄만 바꿔서 패닉을 부를 수 있다.** 실제로 `StartbitV2_patched.ts:324`에서 한 번
  제거했다가 RESULT 기능을 추가하면서 같은 호출이 재유입된 **회귀 전력**이 있다. 원래 의도했던 톤
  패턴(correct=880Hz 1초 / incorrect=880Hz 200ms 2회)은 주석으로만 기록해 뒀다.
- `showResult()`는 `basic.pause(2000)`으로 LED 표시 2초를 그대로 유지한다.
- 최상단에 **🔴 절대 규칙 블록**을 배치했다 — 파일을 여는 사람이 가장 먼저 보게 된다.

**산출물 반영 6곳**
- `services/actuation/src/firmware/aihand_control.ts` — 규칙 블록 신설, `music` 호출·플래그 삭제
- `document/11_하드웨어설계서_v1.md` §4.7 (2) — 제목을 "부저 비활성화" → **"BLE와 `music` 병용 불가"**
  로 바꾸고 근거표·철회 고지·규칙 2항 기재
- `document/03_인터페이스계약서_v2.md` §5-3 — 같은 내용으로 교체, 부활 단서 철회 명시
- `shared/schemas/microbit_protocol.md` — `correct`/`incorrect` 항목의 부저 주석 갱신
- `services/actuation/README.md` — 절대 규칙으로 승격
- `document/08_리스크레지스터.md` — **신규 행 등록**(방침 확정). 회귀 전력이 있어 "코드가 아니라
  규칙으로 못박는다"는 점을 근거에 남겼다

**남은 것**
- 패닉 070의 **저전압 경로는 여전히 미해결**이다. 이번 건은 `music` 경로만 종결됐다 — 알려진 이슈
  표를 두 줄로 분리했다.

### 2026-09-22 (추가) — RPi4B·카메라 입수, 임시 구성 폐지 / 1차 벤치 테스트 생략 검토

**전 부품 보유 상태가 됐다**

| 부품 | 이전 | 현재 |
| --- | --- | --- |
| Raspberry Pi 4B (8GB) | ⬜ 미도착 | ✅ 보유 |
| Pi Camera Module 3 | ⬜ 미도착 | ✅ 보유 |

오전에 입수한 Raspbot 차체·외부 LED·UTP까지 합쳐 **미도착 항목이 하나도 없다.**

**§1.4(1차 구현 임시 구성) 폐지**

§1.4는 본문에 *"RPi4B가 도착하면 §1.2 정식 구성으로 되돌린다"* 고 적어 뒀고, 그 시점이 왔다.
§2~§7을 건드리지 않고 별도 절로 분리해 둔 덕에 그 절만 걷어내면 됐다. 함께 폐지된 항목:
§10 #11(1차 구현 Wi-Fi), #12(USB 웹캠 확보), §7.1의 "RPi5를 5V/3A로 돌려도 되는가" 계산.

- 🔴 **폐지하면 안 되는 것 2건을 명시해 뒀다** — **전원 계통 분리(§7.1)와 헤더 5V 백피드
  확인(§10 #13)** 은 보드와 무관하게 유효하다. RPi4B도 헤더 5V 핀에는 역류 방지가 없다.
  §1.4를 걷어내면서 함께 지우기 쉬운 자리라 경고를 남겼다.
- **폐지해도 남는 소득 3가지**를 표로 정리했다: `gpiozero` 채택 덕에 보드를 바꿔도 코드가 그대로
  돈다는 점, `bleak`이 크로스플랫폼이라 PC에서 BLE만 따로 검증할 수 있다는 점(A-1에서 실제로 활용),
  Windows Docker가 웹캠·BLE를 전달하지 못한다는 점.

**§7.1 전원 계산을 RPi4B 기준으로 교체**

RPi4B의 공식 요구가 **USB-C 5V 3A**라 보유 어댑터가 곧 정격이다. RPi5용으로 따지던 "27W 요구의
정체는 USB 주변기기 1.6A" 같은 논의가 통째로 불필요해졌다. 5V/5A 요구는 고정 스테이션 쪽에만 남는다.

**1차 벤치 테스트를 건너뛰고 2차(바닥 주행)로 바로 갈 수 있는지 — 검토 결과: 건너뛰지 않는다**

건너뛰자는 근거(I2C 프로토콜은 제작사 이미지로 확증됨, 자동 정지 구현돼 있음, 일정 압박)는
이해되지만, **셋 다 아직 실물에서 한 번도 확인된 적이 없다.**

| 미확인 항목 | 바닥에서 틀렸을 때 |
| --- | --- |
| 바퀴 회전 방향(좌우 배선) | `forward`가 제자리 회전이 된다 — 책상에서 떨어질 수 있다 |
| **2초 자동 정지의 실동작** | `_safe_stop()`이 I2C 예외를 `except: pass`로 **조용히 삼킨다.** 정지에 실패해도 **로그조차 없이 계속 달린다** |
| I2C 주소(`0x16` vs `0x2B`) | 틀리면 무반응. 무해하지만 바닥에서 알 이유가 없다 |

추가로 `_auto_stop_timer`는 **daemon 스레드**다. 주행 중 서비스를 `Ctrl+C`로 죽이면 타이머가
사라지고 코프로세서는 **마지막 명령을 계속 유지**한다 — 바퀴가 멈추지 않는다. 블럭 위에서는 이
동작을 안전하게 관찰할 수 있다.

- **비대칭이 결정적이다.** 블럭 테스트는 10~15분이고, 막는 사고는 **차체 또는 RPi4B 손실**이다.
  RPi4B는 예비가 없고 오늘 막 도착했으며, 남은 일정은 W4(~10/04)다.
- 다만 **별도 일정으로 분리할 필요는 없다.** 같은 세션의 앞 10분으로 붙이면 되고, 오히려 방향
  매핑을 미리 알고 들어가므로 2차 튜닝이 빨라진다. → §9.2에 근거와 함께 명시했다.

**산출물 반영**
- `11_하드웨어설계서_v1.md` v1.6 — §1.2·§1.4·§2.2·§3·§6·§7·§7.1·§9·§9.2·§10·§11
- `시스템_구성도_초안.md` v5 — §6 폐지, §1·§4 갱신
- `10_PRD_v2.md` — §11.2 "picar 전원 계통 분리 여부"·"Wi-Fi IP 구성" 2건 해소 처리, 머리말 갱신
- `08_리스크레지스터.md` — RPi5 리소스 실측 항목의 선행 조건(카메라 도착) 해소

**🟡 타 담당 문서에 남은 "미도착" 기술** (vision 담당 확인 필요 — 직접 수정하지 않음)
- `05_모델카드_v3.md` §8 — "카메라가 아직 도착 전이라 추론 속도 미측정"
- `services/vision/README.md`, `services/vision/MODEL_TRAINING.md` — `MOCK_CAMERA=true` 기본값 근거
- `shared/README.md` — "실물 도착 후 재검증 예정"

**다음 할 일**
- [ ] 🔴 조립 전 확장보드 **헤더 5V 공급 구조 확인** (백피드 차단, GND 공통 유지)
- [ ] RPi4B에 Bookworm 설치 + `dtparam=i2c_arm=on` → `i2cdetect -y 1`로 `0x16` 확인
- [ ] 외부 LED 4개 배선 후 **블럭 위 3단계 벤치 테스트** (§9.2)
- [ ] 이어서 바닥 주행(2차) — 속도 실측, 좌우 회전 튜닝

### 2026-09-22 (추가) — picar 전원을 차체 배터리 단일 계통으로 변경 (오전 결정 번복)

**결정**: RPi4B는 **별도 어댑터를 쓰지 않는다.** 차체 배터리가 강압을 거쳐 **GPIO 헤더 5V로 Pi에
공급**한다. 모터와 Pi가 같은 배터리를 공유하는 단일 계통이다.

| 배터리 사양 | 값 |
| --- | --- |
| 구성 | 3S 리튬이온 — 공칭 **11.1V** / 만충 12.6V / **하한 9.0V**(3.0V/셀) |
| 용량 | **2200mAh · 24.42Wh** |

**① 용량은 제약이 아니다**

가용 19.5Wh(24.42의 80%), RPi4B 헤드리스 약 3W에 강압 효율 85% 적용:

| 조건 | 평균 | 구동 시간 |
| --- | --- | --- |
| Pi 단독 | 3.5W | 약 5.5시간 |
| + 모터 듀티 10% | 5.2W | **약 3.8시간** |
| + 모터 듀티 20% | 6.9W | 약 2.8시간 |

주행은 명령당 2초뿐이라 실제 듀티가 10%를 넘기 어렵다. **시연 시간 대비 충분하다.**

**② 🔴 진짜 제약은 과도 특성이다**

모터 기동/정지 순간의 전압 흔들림이 위험하다. **RPi4B는 4.63V 미만을 언더볼티지로 판정**하고,
그 상태에서 SD에 쓰고 있으면 카드가 깨진다. 확인할 것 3가지를 §7.1.2에 정리했다 —
차체 강압 회로의 5V 정격(부팅 돌입 전류), 벌크 커패시터(470~1000µF) 추가 여부, LVC 유무.

**③ 되살아난 리스크 — 모터 노이즈 ↔ Wi-Fi**

오전의 계통 분리로 "레일을 공유하지 않으니 경로 자체가 없다"고 해소 처리했던 항목이다.
**단일 계통에서는 다시 실측 대상**이며, picar가 Wi-Fi로 명령을 받으므로 끊기면 시연이 멈춘다.
→ `08_리스크레지스터`에 신규 등록하고, 구성도 §5의 체크도 되돌렸다.

**④ 🔴 배선 규칙이 반전됐다 — 가장 헷갈리기 쉬운 지점**

| | 오전(분리) | **현재(단일)** |
| --- | --- | --- |
| 헤더 5V | **차단할 것** | **급전 경로 — 연결 유지** |
| USB-C 어댑터 | Pi 급전용 | 🔴 **동시 연결 금지** |
| GND | 공통 유지 | 공통 유지 (동일) |

백피드 위험이 사라진 것이 아니라 **"차단하라"가 "둘 중 하나만 쓰라"로 바뀐 것**이다. 개발 중
어댑터를 쓰려면 배터리 스위치를 내리고 꽂는다.

**⑤ 얻은 것** — 어댑터 테더링이 사라져 **주행 시연이 가능**해졌다. §10 #14(주행용 PD 보조배터리
확보)는 불필요해졌다.

**산출물 반영**: `11_하드웨어설계서_v1.md` v1.7(§7 계통표·§7.1 전면 재작성·§9.2·§10 #8·#13·#14
갱신 및 #15~#17 신규), `시스템_구성도_초안.md` §5, `10_PRD_v2.md` §11.2, `08_리스크레지스터.md`
신규 행, `services/picar/README.md`.

**다음 할 일**
- [ ] 차체 강압 회로 5V 출력 정격 확인 (부팅 돌입 전류를 받치는지)
- [ ] 배터리 LVC 유무 확인 — 없으면 주행 전 전압 측정 습관화 (9.0V 하한)
- [ ] 벤치에서 `vcgencmd get_throttled` + 모터 구동 중 Wi-Fi 왕복 실측
- [ ] 결과에 따라 5V 레일 벌크 커패시터(470~1000µF) 추가 판단

### 2026-09-22 (추가) — 적색 LED를 2핀으로 분리 (5mm 사양 확정에 따름)

**결정**: LED는 **5mm(정격 20mA)**, 저항은 개별 직렬로 통일(당초 330Ω → **같은 날 220Ω으로 변경**,
아래 후속 참고). 그에 맞춰 **적색 2개를 BCM13 하나의 병렬에서 BCM13 + BCM19 두 핀으로 분리**한다.

**왜 분리했나**

| 구성 | 330Ω 기준 핀 전류 | 판정 |
| --- | --- | --- |
| 적색 2개 → 1핀(병렬) | 약 **7.9mA** | ⚠️ 핀 기본 구동 한도 **8mA에 붙음** |
| 적색 2개 → **2핀** | 핀당 약 **3.9mA** | ✅ 핀당 8mA까지 여유 |

5mm LED 정격이 20mA라 330Ω(약 3.9mA)은 **정격의 20%** 수준이다. 실물에서 어두우면 저항을 낮춰야
하는데, **병렬 구성은 이미 한도에 붙어 있어 그 여지가 없었다**(220Ω이면 약 12mA로 초과).
GPIO 구동 강도 설정을 바꾸는 것은 뱅크 단위라 다른 핀에 영향이 가고, 트랜지스터 추가는 부품이
늘어난다. **핀을 나누는 것이 가장 간단하다** — BCM19는 Raspbot 점유가 아닌 여유 핀이다.

**스키마는 바뀌지 않는다.** 적색은 정지(양쪽 점등)·확인_완료(양쪽 점멸)에만 쓰이고 한쪽만 켜지는
경우가 없어 `red` 단일 채널 그대로다. 핀이 2개가 된 것은 **전류 문제일 뿐 제어 의미는 1채널**이다.

**코드 변경** (`services/picar/src/controller.py`)
- `PIN_MAP` 값을 **핀 튜플**로 바꿨다 — `led_red: (13, 19)`, 황색은 `(5,)`/`(6,)`. 핀이 1개인
  채널도 튜플로 통일해 분기를 단순하게 뒀다.
- 🔴 **`LED` 대신 `LEDBoard`를 쓴다.** `LED` 객체 2개를 따로 `blink()`시키면 **각자 스레드를 돌려
  위상이 어긋날 수 있다** — 적색 2개가 엇갈려 깜빡이면 "확인_완료" 표시가 망가진다. `LEDBoard`는
  한 스레드로 묶어 구동하므로 동조가 보장된다. 핀을 나누면서 새로 생긴 위험이라 코드로 막았다.
- 응답 필드가 `pin`(정수) → `pins`(리스트)로 바뀌었다. web은 이 필드를 쓰지 않아 영향 없음.
- 테스트 27개 통과(+1) — `test_red_channel_drives_two_dedicated_pins`로 **다시 1핀으로 합치는
  회귀를 막는다.**

**산출물 반영**: `11_하드웨어설계서_v1.md` §5.3·§7·§9.2, `hardware_pinmap.md`(여유 핀 목록 포함),
`02_설계문서_v2.md`, `services/picar/README.md`.

**참고 — 여유 핀이 하나 남았다**: 이제 완전히 자유로운 핀은 **BCM26** 하나뿐이다.

**후속 (같은 날) — 저항 330Ω → 220Ω**

실물에서 어두워 저항을 낮추기로 했다. **핀을 분리해 둔 덕에 가능한 선택**이었다 — 병렬 구성이면
220Ω에서 약 11.8mA로 핀 한도 8mA를 넘었을 것이다.

| 저항 | 적색 | 황색 | 판정 |
| --- | --- | --- | --- |
| 330Ω | 3.9mA | 3.6mA | 어둡다 (5mm 정격 20mA의 20%) |
| **220Ω** | **5.9mA** | **5.5mA** | ✅ 채택 — 핀 기본 한도 8mA 안 |
| 150Ω | 8.7mA | 8.0mA | ❌ 적색이 한도 초과 |

전체 4핀 합계 약 22.7mA로 보드 한계(50mA) 안이다. **150Ω 이하로는 내리지 않는다**는 기준을
문서에 박아 뒀다 — 더 밝게 하려면 저항이 아니라 트랜지스터 드라이버가 필요하다.

- ✅ **220Ω 4개 보유 확인** (2026-09-22). 부품 측 대기 항목 없음 — **배선·점등 검증만 남았다.**
- **GND 배치 확정**: 황색 2개는 물리 30번, 적색 2개는 물리 34번에 **쌍으로 묶는다.** 각 GND가
  자기 쌍의 한가운데(29·31 사이 / 33·35 사이)라 배선이 가장 짧고 그라운드 선이 4→2가닥이 된다.
  GND 핀은 보드 내부에서 모두 연결돼 있고 **8mA 한도는 출력 핀에만 적용**되므로 합산 약 12mA는
  무방하다. ⚠️ **GND 공유와 저항 공유는 다르다** — 저항은 LED마다 하나씩 유지 (§5.3).

### 2026-09-23 — 🎉 picar 1차 벤치 테스트 전 항목 통과

블럭 위(바퀴 띄움)에서 1~7단계를 모두 확인했다. **picar 하드웨어 경로가 실물에서 처음으로
end-to-end 검증됐다.**

| 단계 | 항목 | 결과 |
| --- | --- | --- |
| 1 | I2C 코프로세서 `0x16` | ✅ `/health` = `ok`, `reachable: true` |
| 2 | 외부 LED 4개 / 3채널 | ✅ 적색 2개 동조, 황색 좌·우 단독, `blink` 정상 |
| 3 | 바퀴 방향 (전진·좌·우·후진) | ✅ 4항목 전부 의도대로 — **배선 반전·좌우 교체 불필요** |
| 3 | **2초 자동 정지** | ✅ **실동작 확인** |
| 6 | `vcgencmd get_throttled` (모터 20회 후) | ✅ **`0x0`** |
| 6 | 모터 구동 중 Wi-Fi 왕복 | ✅ **실패 0건** |

**① 전원 단일 계통 — 최대 불확실성이 해소됐다**

어제 전원을 배터리 단일 계통으로 바꾸면서 되살아났던 두 리스크가 **실측으로 닫혔다.**

- `get_throttled`가 `0x0`이고 **bit 16(발생 이력)까지 서지 않았다** — 순간 강하조차 없었다는 뜻이다.
  차체 강압 회로가 Pi와 모터를 여유 있게 감당한다.
- **벌크 커패시터 불필요**(§10 #17 종결), 강압 회로 정격 확인도 해소(#15).
- 모터 노이즈가 Wi-Fi에 영향을 주지 않았다 — `08_리스크레지스터` 해당 행을 🟡 → 🟢으로 내렸다.

> 🟡 **단서: 무부하 조건이었다.** 바퀴를 띄워 모터가 차체 무게를 밀지 않았다. 바닥에서는 기동
> 토크로 전류가 크게 늘고, 배터리가 12.6V → 9.0V로 갈수록 강압 여유가 준다 — **시연 후반부가
> 가장 불리하다.** §10 #18로 "바닥 주행 재확인"을 남겼고, 커패시터도 배제하지 않고 "플래그가
> 서면 재검토"로 열어 뒀다.

**② 자동 정지가 실제로 동작한다**

`_safe_stop()`이 I2C 예외를 `except: pass`로 삼키는 구조라, **정지에 실패해도 로그조차 안 남는다.**
이것이 1차 벤치를 건너뛰지 않기로 한 가장 큰 이유였는데, 실물에서 정상 동작을 확인했다.
바닥 주행의 선결 조건이 충족됐다.

**③ 디버깅에서 배운 것 — 기록해 둘 가치가 있다**

LED가 처음에 전혀 안 켜졌고, 원인은 **GND 오삽입**이었다. 좁혀 간 경로:

| 관찰 | 판단 |
| --- | --- |
| `POST /picar` 응답이 `ok`(≠`mocked`) | 소프트웨어·`MOCK_HARDWARE` 정상 |
| `pinctrl set 13 op dh`로도 안 켜짐 | **소프트웨어 문제 아님 — 물리 쪽** |
| `pinctrl get 13` = `hi` | SoC는 3.3V를 내고 있음 → 핀 이후 구간 문제 |
| **4개 전부** 실패 | 개별 불량이 아니라 **공통 원인** → GND 의심 |

- 🔑 **`pinctrl`로 서비스를 우회하면 SW/HW가 한 번에 갈린다.** 서비스를 끄고 핀만 직접 때려 보는
  것이 가장 빠른 분리 방법이다.
- 🔑 **"전부 실패 = 공통 원인"** — 개별 배선을 하나씩 뜯기 전에 GND·전원·핀 기준점부터 본다.
- ⚠️ **`pinctrl` 잔상 주의**: `dh`로 올려둔 핀은 서비스를 다시 띄우기 전까지 그대로 남는다.
  GND를 고친 직후 "LED가 안 꺼진다"고 판단했던 것이 이 때문이었다 — 코드 문제가 아니었다.
  (gpiozero 2.0.1 소스로 `LEDBoard.off()`가 `_stop_blink()`를 먼저 부르는 것까지 확인했다.)

**산출물 반영**: `11_하드웨어설계서_v1.md` v1.9(§9.2 결과 요약표·체크리스트, §5.2·§5.3 ✅,
§7.1.2·§7.1.3 실측 반영, §10 #1·#15·#17 종결 / #18 신규), `08_리스크레지스터.md`.

**다음 — 2차(바닥 주행)**
- [ ] 바닥에서 전진/좌/우/후진 — **부하 조건**에서 방향·자동 정지 재확인
- [ ] 같은 조건에서 `get_throttled`·Wi-Fi 재측정 (§10 #18)
- [ ] **모터 속도 실측값 확정** (현재 서행 40 / 좌우회전 60 / 후진 50 잠정 — §10 #5)
- [ ] 주행 지속 시간 확정 → 스키마 `duration_ms` 추가 여부 결정 (proposals 결정 1)
- [ ] 배터리 LVC 유무 확인 (§7.1.2)

### 2026-09-23 (추가) — 바닥 부하 테스트: 모터 속도 실측값 확정

**결과**

| 구분 | 값 |
| --- | --- |
| `speed: 60` | ❌ **너무 빠름** (기존 좌/우회전 값) |
| **상한선** | **50** — 어떤 신호에도 배정하지 않고 여유로 남긴다 |
| **일반 주행** | **40** — 좌회전·우회전·후진 |
| **서행** | **20** |

종전 잠정값(서행 40 / 좌우회전 60 / 후진 50)은 **전반적으로 빨랐다.** 특히 좌우회전이
제자리 회전이라 60에서는 급격하게 돈다.

- 🔑 **상한 50은 사용값이 아니라 여유다.** 바닥 상태·배터리 잔량에 따라 실제 속도가 달라지므로,
  상한에 붙여 두면 조건이 나빠졌을 때 물러설 곳이 없다.
- 주신 값이 3개(상한/일반/서행)이고 이동 신호는 4종이라, **좌회전·우회전·후진에 "일반 주행 40"**
  을 적용하는 것으로 읽었다.

**🟡 적용은 web 담당(조은수)이다**

속도 값은 `services/picar`가 아니라 **`services/web/backend/state_machine.py`의 `PICAR_COMMANDS`**
에 있다. 담당 파일이라 직접 고치지 않고, 바꿀 4줄을 그대로 적어 전달한다
(`13_picar_하드웨어_검증리포트_v1` §4.5).

`shared/schemas/picar_command.schema.json`의 `speed` 설명에 남은 *"⚠️ 실측 후 확정"* 표기도
정리 대상이지만, `shared/` 변경은 전원 리뷰 대상이라(`Git_설계서_v1`) 기록만 남겼다.

**산출물 반영**: `13_picar_하드웨어_검증리포트_v1` §4.5 신설·§7.2 #2 종결,
`11_하드웨어설계서_v1` v1.10(§10 #5 확정).

**다음 할 일**
- [ ] 조은수에게 속도값 적용 요청 (4줄)
- [ ] §4.1 시행 로그 표 채우기 — `get_throttled`·실패율·지연 P95·신호 세기
- [ ] 주행 지속 시간 확정 → `duration_ms` 스키마 추가 여부 (proposals 결정 1)

### 2026-09-23 (추가) — 🔴 부하 테스트가 **정지 실패 결함**을 드러냄 (시연 사고 직전에 잡음)

#### 부하 테스트 결과 (10회차, 누적 구동 약 10분)

| 항목 | 결과 |
| --- | --- |
| `vcgencmd get_throttled` | **10회 전부 `0x0`** (`speed 70` 포함) |
| 요청 실패 | **653회 중 0건** |
| 왕복 지연 P95 | **43~49ms** — 계약 예산 500ms의 **1/10** |
| 무선 신호 | -27 ~ -54 dBm (양호) |

→ **전원 단일 계통은 부하 조건에서도 안전하다.** 무부하와 부하 모두 통과했으므로 벌크 커패시터는
불필요하고, 설계서 §10 #18을 닫았다.

#### 🔴 #10(`speed 50`)에서 "끝날 때 정지 안 됨" — 코드 결함이었다

```python
if kind == "stop":
    _cancel_auto_stop()   # ← 안전망을 먼저 걷고
    _write_stop()         # ← 그 다음에 정지를 쓴다
```

**`_write_stop()`이 실패하면 자동 정지 타이머는 이미 취소된 뒤**라, 차를 멈출 수단이 하나도
남지 않는다. `get_throttled`가 `0x0`이었으므로 **전원이 아니라 I2C 경로 문제**이고, `speed 50`
에서만 난 것은 모터 전류가 클수록 I2C에 노이즈가 실리기 때문으로 보인다.

- ⚠️ **`load_test.py`가 이 실패를 놓쳤다** — 표에 `0/105`로 기록됐다. HTTP 200만 보고 응답
  **본문**을 읽지 않았기 때문이다. picar는 I2C가 실패해도
  `{"status":"partial","motor":{"reason":"i2c_failed"}}` 를 **200으로** 돌려준다.
  **web `_post_with_retry`에 지적했던 것과 똑같은 결함을 내 스크립트가 갖고 있었다.**

#### 조치 5건

| # | 조치 | 위치 |
| --- | --- | --- |
| 1 | **정지 쓰기 성공 후에 타이머를 걷도록 순서 반전** — 실패해도 안전망이 남아 늦어도 2초 안에 멈춘다 | `controller._apply_motor` |
| 2 | **정지 쓰기 재시도 3회** (`PICAR_STOP_RETRY`) | `controller._write_stop_retrying` |
| 3 | **정지 실패 사유를 `/health`에 노출**(`last_stop_error`) | `controller.diagnose` |
| 4 | **응답 본문의 `motor.status`까지 확인** | `scripts/load_test.py` |
| 5 | **회귀 테스트 4건** — 순서를 되돌리면 2건이 실패하는 것까지 **변이 검증 완료** | `tests/test_controller.py` |

- 📌 **`motion_active`는 타이머 상태일 뿐 바퀴가 도는지가 아니다.** 정지 쓰기가 실패해도 타이머가
  없으면 `False`가 나온다 — `last_stop_error`를 함께 봐야 한다. 리포트와 코드 주석에 명시했다.
- 🔑 **부하 테스트를 건너뛰었다면 시연 중에 터졌을 결함이다.** 1차 벤치를 생략하지 않기로 한
  판단(2026-09-22)이 여기서도 값을 했다.

#### 오늘 만든 도구 2개

- `services/picar/scripts/load_test.py` — 부하 주행(spin/circle/square) + 지연·실패율 측정.
  `finally`에서 정지 3회 재시도, 종료 시 `/health`로 정지 확인. 구간 길이가 자동 정지(2초)
  이상이면 실행을 거부한다.
- `services/picar/scripts/wifi_watch.ps1` — PC↔AP 구간 신호 기록(PowerShell).
  ⚠️ **현재 이 PC는 유선이라 쓸 일이 없다** — picar 구간은 `ssh`로 `iw dev wlan0 link`를 본다.
  시연 때 RPi5를 AP로 띄우면 PC도 무선이 되므로 남겨 둔다.

**산출물 반영**: `13_picar_하드웨어_검증리포트_v1` v1.1(§4.1 로그·해석, §4.2, §4.5, §5 #5·#6,
§7 결론 재작성), `11_하드웨어설계서_v1`(§10 #18 종결), `08_리스크레지스터.md`.

**다음 할 일**
- [ ] 🔴 조치 후 **`speed 50` 재시행** — 정지가 안정적인지 확인
- [ ] 조은수에게 속도값 적용 요청 (4줄)
- [ ] 주행 지속 시간 확정 → `duration_ms` 스키마 추가 여부 (proposals 결정 1)
- [ ] 이슈 #3·#4 조치 (LED 예외 미포착 / 비정상 종료 시 LED·모터 잔상)

---

## ✅ 전체 TODO (누적)

- [x] ~~모터 I2C 명령 프로토콜 확보 + `services/picar` 실물 구동 구현~~ → 프로토콜 확보·구현 완료
  (2026-09-21). **실물 검증만 남음** — `i2cdetect`로 주소 확인, 바퀴 방향·자동 정지 확인
- [x] ~~모터 속도 실측값 확정~~ → **확정**(2026-09-23): 서행 20 / 좌우회전·후진 40 / 상한 50.
  **web `state_machine.py` 적용은 조은수 담당**
- [ ] picar 주행 지속 시간 확정 (현재 자동 정지 2초 잠정값, 스키마 `duration_ms` 추가 여부 포함)
- [x] ~~제작사 제공 SD 이미지에서 I2C 프로토콜 확증~~ → `YB_Pcb_Car.py` 17개 사본 md5 일치,
  `0x16`/`0x01`·`0x02`·`0x03` 문서와 일치 확인 (2026-09-22, 11_하드웨어설계서 §5.5)
- [x] ~~**RPi5를 Raspbot 배터리로 구동 가능한지 확인**~~ → **무효**. picar 전원 계통을 분리하기로
  결정(2026-09-22) — 모터·서보는 배터리, RPi5는 별도 어댑터. 현재 5V/3A로 충분하다고 판단(USB
  주변기기 미연결), 5V/5A는 배송 중
- [ ] 🔴 조립 전 **확장보드 헤더 5V 공급 구조 확인** → 공급 구조면 차단(백피드 위험), **GND는 공통 유지**
- [x] ~~바닥 부하 테스트(2차)~~ → **통과**(2026-09-23). 누적 10분 `get_throttled=0x0`, 653회 실패
  0건. **단 정지 실패 결함 발견·조치 → `speed 50` 재시행 필요**
- [ ] 🔴 정지 결함 조치 후 **`speed 50` 재시행**
- [x] ~~블럭 위 벤치 테스트 수행~~ → **전 항목 통과**(2026-09-23). I2C·LED·바퀴 방향·자동 정지·
  전원·Wi-Fi 모두 확인. 다음은 바닥 주행(2차)
- [ ] 5V/5A 어댑터 수령 시 교체
- [x] ~~RPi4B Bookworm 설치 + `dtparam=i2c_arm=on` → `0x16` 확인~~ → **완료**(2026-09-23)
- [x] ~~USB 웹캠 확보~~ → **불필요**. Pi Camera Module 3 입수(2026-09-22)로 CSI 직결
- [x] ~~1차 구현 Wi-Fi 구성(RPi5 AP ↔ PC)~~ → **폐지**. RPi4B 입수로 정식 구성(RPi5 AP ↔ picar) 복귀
- [ ] `capture.py`의 picamera2 경로 구현 (현재 `NotImplementedError`) — vision 담당
- [ ] `확인_완료`의 "적색·황색 번갈아 점멸"을 스키마로 표현할지 결정 (현재는 동시 점멸로 축약됨)
- [x] ~~황색 LED 2개 배선 및 BCM 핀 확정~~ → 핀 확정(적색 공통 BCM13 / 황색 좌 BCM5 / 우 BCM6),
  물리 배선은 실물 도착 후
- [x] ~~외부 LED 4개(5mm) 물리 배선~~ → **완료·점등 확인**(2026-09-23). 적색 BCM13·19 / 황색 BCM5·6, 각 220Ω
- [x] ~~AI Hand 서보 전원 경로 확인~~ → 확장보드가 7.5V 어댑터에서 직접 분배하는 구조로 확정
  (2026-09-21). 기존 "micro:bit 300mA" 근거는 틀린 것으로 판명되어 문서 6곳 정정
- [x] ~~확장보드 서보 핀 전압 실측~~ → **7.46V 확정**(2026-09-21), 정격(4.8~6.0V)의 약 124%.
  강압 없음
- [ ] 🔴 여분 LFD-01 서보 확보 (정격 초과 운용에 대한 보험, 추가 고장 대비)
- [ ] 자체 촬영 일정·대상자 확정 (`회의안건_2026-09-21` 안건 1) — KPI 실측의 유일한 경로
- [ ] (협의) AI Hand 시범에서 겹치는 4종(`G3`/`G6`, `G4`/`G7`)을 web 화면으로 구분시키는 방안 — web 담당과
- [ ] `services/web` 프론트엔드 구현 (현재 `main.js` 5줄, SC-01~SC-07 화면 미구현)
- [ ] `services/web` 상태머신 단위 테스트 (현재 0개) — web 담당
- [ ] `services/data/src/collect.py` 구현 (촬영 프로토콜 확정 후)
- [x] ~~picar 전원 계통이 Wi-Fi 모듈에 노이즈 영향 주는지 검증~~ → **전원 분리 결정으로 선제 해소**
  (2026-09-22). 모터와 Pi가 레일을 공유하지 않으므로 경로 자체가 사라짐. 실물에서 재확인만 하면 됨
- [x] ~~RPi5 ↔ RPi4B Wi-Fi IP 구성(고정 IP/mDNS)~~ → **AP 모드 1:1 직결 + 고정 IP로 확정**
  (2026-09-21, 11_하드웨어설계서 §6.1). 실물 구축만 남음
- [ ] web↔picar 통신 신뢰성 개선안 협의 (조은수) — `web_picar_통신_신뢰성_개선안.md`
- [ ] RPi5(4GB) 런타임 리소스 실측(메모리/CPU/MediaPipe fps) + 액티브 쿨러 장착 확인
  (08_리스크레지스터 신규 항목)
- [x] ~~엄지 서보 교체 및 재캘리브레이션~~ → 교체하지 않기로 확정(2026-09-21)
- [x] ~~`/result`↔펌웨어 RESULT 연동, PROGRESS 프로토콜 구현~~
- [x] ~~`services/web` 상태머신 ↔ vision/actuation/picar 연동~~ (mock 기준 end-to-end 확인)
- [x] ~~PRD·인터페이스 계약서(§3.2, `microbit_protocol.md`) BLE·G4 기준 갱신~~
- [x] ~~vision 서비스 Dockerfile import 경로 버그 확인~~ → 해당 없음

## 🐛 알려진 이슈 (누적)

| 이슈 | 발생일 | 상태 | 비고 |
|---|---|---|---|
| 엄지 서보(Hiwonder LFD-01) 하드웨어 고장 | 2026-09-21 이전 | 🟢 종결(결정) | 스왑 테스트로 고장 확정. **교체하지 않기로 확정**하고 엄지 동작은 설계·시현에 유지 — 물리적으로만 안 움직이는 상태로 진행 |
| micro:bit 패닉코드 070(SD_ASSERT) — **`music` 경로** | 2026-09-21 이전 | 🟢 종결(규칙 확정) | **원인 확정: BLE SoftDevice와 `music` 라이브러리가 같은 하드웨어 타이머/PWM 자원을 공유**한다. 튜닝으로 우회 가능한 버그가 아니다(2026-09-22 전용 검증). → **규칙: `bluetooth.startUartService()` 이후 `music.*` 호출 금지**, 소리 자리는 LED로 대체. 펌웨어에서 `playResultTone()`·`BUZZER_ENABLED` **삭제** 완료 |
| micro:bit 패닉코드 070(SD_ASSERT) — **저전압 경로** | 2026-09-21 이전 | 🔴 미해결 | 엄지 서보발 전압 흔들림이 원인으로 추정. 시현은 다른 기계를 사용하고, 직접 진행하는 테스트는 4개 손가락으로만 진행 |
| PRD 우회전_유도(G4) 설명과 펌웨어 불일치 | 2026-09-21 | 🟢 해결 | 펌웨어("엄지+소지")가 정답으로 확정, PRD §3.2 정정 완료 |
| vision Dockerfile 임포트 경로 의심 버그 | 2026-09-21 | 🟢 해당 없음 | 확인 결과 이미 `--app-dir src` 적용되어 있었음 |
| `G3`↔`G6`, `G4`↔`G7`가 엄지값만 달라 AI Hand 시범에서 구별 불가 | 2026-09-21 | 🟡 감수(실측 확정) | **2026-09-21 실물 육안 대조로 구별 불가 확인.** 엄지 유지·재설계 안 함으로 결정 — web 화면의 `target_signal` 표시로 보완(판정 경로·KPI에는 영향 없음) |
| web 상태머신이 수신호 전환 시 vision `POST /reset`을 호출하지 않음 | 2026-09-21 | 🔴 미해결 | N프레임 누적이 초기화되지 않아 직전 판정이 이월될 수 있음 (web 담당 확인 필요) |
| web 상태머신에 `camera_fail`(SC-04)·`certificate`(SC-06)·`reentry`(SC-07) 전이 코드 없음 | 2026-09-21 | 🔴 미해결 | vision이 `reason=no_hand`를 주지만 사용하지 않음 (web 담당 확인 필요) |
| picar 전원 분리 시 GPIO 헤더 5V 백피드 위험 | 2026-09-22 | 🟡 미검증 | RPi5를 별도 어댑터로 먹이면서 확장보드도 헤더로 5V를 공급하면 두 소스가 한 레일에서 맞물린다. RPi5는 USB-C 입력에만 역류 방지가 있고 헤더 5V 핀에는 없다. **조립 전 확장보드 5V 공급 구조 확인 + 해당 라인 차단**, GND는 I2C 기준 전위이므로 공통 유지 |
| PRD §2.1 KPI 표가 "7종+negative=8클래스" 기준 | 2026-09-21 | 🟡 진행중 | 회의 안건 2에서 **7클래스+τ 미판정**으로 변경됨 — KPI 표 정정 필요 |

## 📚 참고 자료

- `document/README.md` — **문서 전체 색인** (2026-09-22 통합 내역 포함)
- `document/10_PRD_v2.md` — 종합 기획서 (KPI, 마일스톤, §3.2 수신호 매핑 표)
- `services/actuation/doc/aihand_gesture_checklist_final.md` — micro:bit BLE 디버깅 세션 1~12 전체 이력
- `document/11_하드웨어설계서_v1.md` 부록 A~C — Raspbot·micro:bit 핀맵, 보드/서보/카메라 원본 스펙
  (종전 `hardware_pinmap.md`·`hardware_spec.md`가 2026-09-22 이 문서로 흡수됨)
- `shared/schemas/` — 서비스 간 인터페이스 계약 (JSON Schema)
