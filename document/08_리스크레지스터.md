# 리스크 레지스터 (상시 갱신)

> 관련: 01_프로젝트계획서_v4
계획서에서 분리하여 팀원 누구나 상태(status)를 업데이트할 수 있도록 관리합니다.
> 

> **이 문서 작성법**
> 
> - 정기 회의(주 2회)마다 5분만 시간을 내어 이 표를 함께 훑어보세요. “발생 가능성/영향”이 바뀌었으면 수정하고, “상태”를 갱신합니다.
> - 상태값은 `미착수 → 확인 중 → 대응 중 → 방침 확정/해결` 순으로 사용하세요. 한 번 “해결”로 표시했다고 지우지 말고 남겨두면, 결과보고서의 “시도와 실패 과정” 근거 자료로 그대로 쓸 수 있습니다.
> - 새로운 리스크는 발견한 사람이 바로 그 자리에서 추가하는 것을 원칙으로 하세요(회의 끝나고 잊어버리기 쉽습니다).

---

| 리스크 | 발생 가능성 | 영향 | 대응 방안 | 상태 |
| --- | --- | --- | --- | --- |
| W3 추석 연휴로 구현 작업일 감소 | 높음 | 구현 지연 | W1+W2 통합 운영으로 시간 확보 후 W3에 재배치 | 대응 중 |
| 미션맵 재료 09.18 마감 지연 | 중간 | 재료 수령 지연 → 구현 착수 지연 | 시연 시나리오를 조기 확정하여 재료 목록 역산, 09.18 이전 여유 있게 제출 | 미착수 |
| 회의 시 안건 과다로 결론 미도출 | 높음 | 팀 전체 방향성 혼선 | 안건별 시간 배분 준수, 미결 안건은 잠정치로 정하고 다음 회의에서 조정 | 대응 중 |
| 재료 주문/사용 중 문제 발생 | 낮음~중간 | 예산 초과, 일정 지연 | 문제 발생 시 팀 자체 해결 대신 즉시 운영사무국에 알림 | - |
| 목표치 미확정 상태로 구현 진입 | 중간 | 구현 범위 불명확 | 잠정 목표치로 우선 진행 후 멘토 1회차 피드백 반영하여 조정 | 대응 중 |
| 소통 부재로 인한 진행 불가 | 높음 | 구현 지연 | 노션을 통해 팀원별 할일 지정 및 시간 확정, 미완료 시 사유 확인 후 재조정 또는 작업 분리 | 대응 중 |
| 카메라가 랜드마크 좌표를 못 내줌 | 낮음 | 아키텍처 전면 수정 | Raspberry Pi Camera Module 3(CSI)로 확정, RPi5에서 MediaPipe 실행 — WonderCam류 온보드 분류 카메라는 채택하지 않음 (2026-09-18) | 해결 |
| 공개 데이터셋에 산업 안전 수신호가 없음 | 높음(거의 확정) | 학습 불가 | **해결(2026-09-20)**: ASL 지문자 데이터셋에서 "의미"가 아닌 **손모양만** 차용해 7종 27,735건 확보. 단 손모양 2건은 데이터에 맞춰 변경(02 §4 참고사항 5) | 해결 |
| 데이터셋 라이선스 문제 | **중간~높음** | 발표/공개 제약, copyleft 의무 전파 | 사용 데이터 3종 중 **ODbL·GPL 2.0 2종이 동일조건공유(copyleft)**. ① 원본 이미지를 저장소에 올리지 않음(재배포 회피) ② 출처·라이선스 발표자료 명시 ③ **학습된 모델 가중치 공개 범위는 팀 논의 후 결정** (04_데이터셋명세서_v2 §5) | 대응 중 |
| ~~negative 클래스 학습 데이터 0건~~ | 확정(실측) | 7종 외 자세를 분류기가 배우지 못함 → 미판정률 KPI(≤5%) 압박 | **해결(2026-09-21 회의 안건 2 A)**: ① **7클래스로 학습**하고 negative는 학습 클래스에서 제외 ② τ 미달을 미판정으로 처리하고 그 출력 라벨로만 `negative` 사용 ③ 미판정률은 τ로 조절(05 §8-1 절차). 01_프로젝트계획서_v4 KPI 문구도 "7종 + τ 미달 시 미판정"으로 정정. 촬영 시 "애매한 자세" 몇 컷을 남겨 8클래스 전환 여지는 유지 | 방침 확정 |
| 🔴 **치명 오분류 1건 발생** (정지→타 클래스) | 확정(실측) | KPI 목표 0건 **미달** | 정지 308장 중 1건(0.3%). 05_모델카드_v3 §8-1 절차 3 **"정지 전용 τ 상향"** 적용 검토. 아래 클래스 불균형과 직결 | 확인 중 |
| **클래스 장수 불균형 18배** | 확정(실측) | 데이터 적은 클래스 정확도 저하 — 하필 정지(308장)가 치명 오분류 KPI 대상 | 학습 시 `class_weight="balanced"` 적용(그리드서치 포함) 또는 `build_dataset.py --per-class`로 상한 | 대응 중 |
| 공개 데이터로는 **인물 단위 분할 불가** | 확정 | 공개 데이터 정답률(97.7%)을 KPI 근거로 쓸 수 없음 | 촬영자 정보가 없어 `subject_id`가 전부 `public`. **KPI 실측은 자체 촬영(외부인 1~2명) 데이터로만 판단** (04 §4, 05 §7-2) | 방침 확정 |
| 팀원 4명만으로 수집 → 과적합 | 높음 | 실제 정답률 급락 | 인물 단위 분할(subject-wise split) 강제. 외부인 1~2명 테스트 데이터 확보 | 대응 중 |
| 부가 기능(QR 로그인·DB)이 필수 기능 일정을 잠식 | 중간 | 핵심 미완성 | 필수(인식+피드백) 완성 전까지 부가 기능 코드 동결 | 방침 확정 |
| 수신호 등록(신규 수신호 실시간 추가) 기능 범위 포함 여부 | - | 분류기 구조와 기능 요구사항 불일치 | 경량 분류기(SVM)는 고정 클래스만 예측 가능해 재학습 없는 실시간 등록이 불가능 → **범위에서 제외 결정** (01_프로젝트계획서_v4 §범위, 03_인터페이스계약서_v2 §6) | 방침 확정 |
| micro:bit ↔︎ RPi 통신 불안정 | 낮음~중간 | 시연 실패 | ~~USB 시리얼 고정, 무선 미사용~~ → **BLE(Nordic UART)로 전환**(2026-09-21). micro:bit v2 하드웨어 UART가 1개뿐이라 서보 초기화 시 USB 시리얼이 죽는 것이 실측으로 확인됨. 2026-09-21 실물 검증에서 `/command`·`/result`·`/progress` 왕복 정상 확인. 통신 두절 시 화면 폴백 UI는 유지 | 방침 확정 |
| **(신규)** 🔴 **micro:bit BLE와 `music` 라이브러리 병용 불가 (패닉 070 / SD_ASSERT)** | 확정(전용 검증) | 소리 피드백 시도 시 **그 자리에서 BLE 두절 + 펌웨어 패닉** — 시연 중 발생하면 AI Hand·판정 표시가 동시에 멈춘다 | micro:bit v2에서 BLE SoftDevice와 `music`이 **같은 하드웨어 타이머/PWM 자원을 공유**하는 근본 제약이다. 2026-09-21 `/result` 테스트에서 최초 재현, 2026-09-22 부저 코드를 직접 넣어 실행하는 전용 검증으로 확정. **타이밍 튜닝으로 우회 불가** — 종전 문서 4곳의 "비블로킹 재생 등을 검증하면 되살릴 수 있다"는 단서는 철회했다. → **규칙: `bluetooth.startUartService()` 이후 `music.*` 호출 금지**, 소리 자리는 LED로 대체. 2026-09-22 펌웨어에서 `playResultTone()`·`BUZZER_ENABLED`를 **삭제**(플래그로 남겨두면 한 줄로 되살아나므로). 과거 `StartbitV2_patched.ts:324`에서 한 번 제거했다가 RESULT 기능 추가 시 재유입된 **회귀 전력**이 있어 코드가 아니라 규칙으로 못박는다 (11_하드웨어설계서 §4.7) | 방침 확정 |
| “서행”·“후진” AiHand 표현이 동일(검지+중지 펴기) | 높음(현재 설계 그대로면 확정) | 두 클래스 분류 불가, 정답률/오분류율 KPI 직접 타격 | 후진을 “검지만 펴기”로 재설계하여 서행과 구분 (02_설계문서_v2 §4 참고사항 2, 2026-09-17) | 해결 |
| picar 신규 하드웨어 통합(모터/LED, RPi4B 8GB 연동) | 중간 | 배선/전원 안정성 | ~~제어 인터페이스는 RPi4B 8GB GPIO 직결~~ → **정정(2026-09-21)**: 모터/서보는 GPIO 직결이 아니라 **하위 MCU 코프로세서를 I2C로 경유**한다(11_하드웨어설계서_v1 부록 A). 명령 프로토콜(주소 `0x16`, reg `0x01`/`0x02`/`0x03`)을 Yahboom `YB_Pcb_Car.py`에서 확보해 `services/picar/src/controller.py`에 구현 완료. LED만 GPIO 직결(적색 BCM21). 배선/전원 안정성은 실물 조립 후 확인 필요 | 대응 중 |
| ~~RPi5 한 대가 카메라 추론과 picar 모터 제어를 동시 수행 → CPU/GPIO 리소스 경합~~ | - | - | **해결**: picar 컴퓨트 보드를 RPi4B 8GB로 분리해 RPi5는 카메라 추론(Perception/Cognition)만 전담하게 됨(02_설계문서_v2 §1-1, 2026-09-18) — 애초에 picar가 카메라와 함께 움직이는 문제를 해결하기 위한 조치였고, 리소스 경합도 부수적으로 해소됨 | 해결 |
| **(신규)** RPi5 ↔ RPi4B(picar) Wi-Fi 통신 지연/끊김 | 중간 | picar 반응 지연·미동작, 물리피드백 지연(P95≤2.0초) KPI 영향 | HTTP 요청 타임아웃 500ms + 1회 재시도, 실패 시 picar 없이 AI Hand+micro:bit로 진행(보조 출력으로 간주) — 03_인터페이스계약서_v2 §5-2·§7 확정 | 방침 확정 |
| `LIVE_STREAM` 콜백과 상태머신 간 스레드 동기화 오류(race condition) | 중간 | 판정 결과 유실/중복, N프레임 누적 로직 오작동 | 콜백→상태머신 결과 전달에 스레드-세이프 큐/락 사용 (05_모델카드_v3 §3-2·§3-6) | 확인 중 |
| **학습(Colab)과 추론(RPi5)의 scikit-learn 버전 불일치** | 중간 | joblib 모델 로드 실패 또는 **조용한 오작동**(예측이 미묘하게 달라짐) | 번들 metadata에 `sklearn_version` 기록 + `model_store`가 기동 시 현재 버전과 비교해 경고. 의존성은 범위 핀으로 두고, 시연 직전엔 `pip freeze`로 고정 (05_모델카드_v3 §6-1) | 방침 확정 |
| mediapipe 레거시 `solutions` API 제거로 폴백 계획 무효화 | 확정(1.x에서 제거됨) | 지연 KPI 미달 시 쓰려던 대안이 사라짐 | 2026-09-19 확인 후 폴백을 재설계 — 추론 입력 축소 / 프레임 서브샘플링 / N축소 / GPU delegate 순으로 시도 (05_모델카드_v3 §3-1). Tasks API 자체는 1.0.1에서도 동일 동작 확인 | 해결 |
| Pi Camera Module 3 CSI 케이블(15핀→22핀 변환) 접촉 불량 | 낮음 | 인식 자체 실패, 시연 중 화면 끊김 | 조립 후 케이블 고정 상태 점검, 예비 변환 케이블 확보 | 미착수 |
| 7종 수신호 원본 자료의 “우회전 유도 → 왼쪽으로 회전” 기재 (오탈자 추정) | 낮음 | 실제 구현 시 좌/우 반대로 동작할 위험 | 원문을 “오른쪽으로 회전한다”로 정정 완료 (2026-09-17) | 해결 |
| “좌회전 유도”·“우회전 유도” AiHand 표현이 손가락 하나 차이(엄지+검지 vs 엄지+약지) | 낮음~중간 | 두 클래스 간 혼동 가능성 | 실측 후 06_테스트·평가리포트_v3 §2-2 혼동행렬에서 확인, 필요 시 τ/N프레임 조정(05_모델카드_v3 §8) | 확인 중 |
| micro:bit 확장보드를 사용할시 rpi5와 시리얼 통신이 불가능한 상황| 높음 | rpi5에서 추론 결과를 통한 수신호를 정보를 못받아옴 | bluetooth를 이용한 통신 | 대응 중 |
| AiHand 엄지 서보 고장(LFD-01) | 확정(발생함) | AI Hand 시범에서 7종 중 4종이 2쌍으로 겹쳐 보임(`G3`↔`G6`, `G4`↔`G7`) | **교체하지 않기로 확정(2026-09-21).** 다만 엄지 동작은 설계·시현에 그대로 유지하고 물리적으로만 멈춘 상태로 진행한다 — 손모양을 바꾸면 PRD §3.2·vision 학습 클래스·공개 데이터까지 연쇄 수정이라 비용이 더 크다. 2026-09-21 실물 육안 대조로 구별 불가를 확정했으나, web 화면이 목표 수신호 이름(`target_signal`)을 표시하므로 교육 흐름은 유지되고 **판정 경로·KPI에는 영향 없음**. 테스트와 시현은 다른 사람 것으로 진행 | 방침 확정 |
| 서보 다수 동시 구동 시 전압강하로 BLE 끊김 | 중간(재현됨) | 통신 두절 시 시연 중단 | 순차 출발 방식으로 동시 부하를 분산 — 종전 200ms 완전 순차에서 **2026-09-23 150ms 간격(인접 2개 50ms 겹침, 약 1.74A < 3A)으로 개정**, RPi5 연속 35동작에서 리셋·끊김 0건으로 확정. 3개 이상 겹치는 간격은 금지. **근거 정정(2026-09-21)**: 원인은 "micro:bit 보드 공급 한계 300mA"가 아니라 **7.5V 3A 어댑터 하나가 확장보드를 거쳐 micro:bit와 서보를 같은 레일에서 먹이는 구조** — 서보 5개 동시 기동 시 3.5A로 어댑터를 넘기면 레일이 주저앉아 micro:bit가 브라운아웃된다(11_하드웨어설계서 §4.4). 실제 제약이 어댑터 용량이므로 전원 보강 시 동시 구동 여지 있음(일정상 후순위) | 해결 |
| **(신규, 2026-09-23)** 🔴 **micro:bit 수신 핸들러 블로킹 — 판정 결과 직후 명령이 timeout** | 높음(실측 재현) | result 직후 `G` 명령이 LED 표시(실제 1.4초) 끝까지 밀려 1.4 + 0.8 ≈ 2.2초 → `ACK_TIMEOUT_S`(2.0초) 초과, 시연 중 손 동작 누락 | ACK-first만으로는 부족했다 — `showResult()`가 핸들러 안에서 `pause`해 다음 명령이 줄을 섰다(연속 실측 1424ms, `showLeds` 기본 400ms 대기 포함). 펌웨어를 `control.inBackground()` + `showLeds(.., 0)`으로 수정. `aihand_test.py`가 result·progress 300ms 초과를 🔴로 자동 표시 (11_하드웨어설계서 §4.5). ✅ **2026-09-25 재검증 통과** — 직후 명령 1424ms → 36~39ms | 해결 |
| **(신규)** 🔴 **서보 정격 전압 초과 운용 (실측 7.46V vs 정격 4.8~6.0V)** | **확정(실측)** | 서보 추가 고장 → 시연 중 손가락 동작 불가. 이미 엄지 1개 고장 | 2026-09-21 멀티미터 실측 결과 확장보드 서보 핀에 **7.46V**가 그대로 인가된다(강압 없음). LFD-01·LD-1501MG 정격은 둘 다 DC 4.8~6.0V이므로 **정격 상한의 약 124%**. **엄지 서보 고장의 유력한 원인이자 나머지 4개도 동일 조건**이다. Hiwonder가 이 서보와 7.5V 어댑터를 한 제품으로 출하하므로 제조사가 의도한 오버볼트로 보이나, 수명 단축은 감수하는 구조다. → **대응: ①여분 LFD-01 확보(1순위, 저가·즉시 가능) ②불필요한 서보 구동 최소화(반복 내구 테스트 지양) ③강압/어댑터 교체는 확장보드 입력 전압 사양 미확인 상태라 권장하지 않음(보드 손실 위험 > 이득).** 가동범위 clamp는 이미 펌웨어에 적용되어 구속 상태는 방어 중 (11_하드웨어설계서 §4.4) | 대응 중 |
| **(신규)** RPi5(4GB) 런타임 리소스(메모리/CPU/MediaPipe 처리율) 미실측 | 중간 | vision/actuation/web 3개 컨테이너 동시 구동 시 메모리 여유 부족 또는 MediaPipe 프레임 처리율 저하 → 판정 지연 P95≤1.0초 KPI 위협. 장시간 시연 시 열 스로틀링 가능성도 있음 | **2026-09-22 카메라 입수 → 즉시 실측 가능(선행 조건 해소).** `docker stats`로 컨테이너별 메모리/CPU 실측, MediaPipe 프레임 처리율(fps) 실측(05_모델카드_v3 §3-3), 액티브 쿨러 장착 여부 확인. **2026-09-25 실측**: CPU 약 37%·메모리 795MB는 여유, 판정 26.6fps. 🔴 그러나 **최고 86.2°C·`get_throttled=0xe0008`(스로틀링 발생)** → 열이 병목. 액티브 쿨러 장착 확정 (11_하드웨어설계서 §2.1, §10 #7) | 대응 중(쿨러) |
| 손가락 한두 개 차이 클래스 쌍의 혼동 | 중간(완화됨) | 오분류율 KPI(≤3%) 압박 | 2026-09-20 실측: 주의↔우회전 유도에 오분류가 몰림(2,530건 = 전체의 41%). **손모양 교체로는 해결 불가**로 판명(32자리 중 24자리가 막혀 쓸 자리가 8개뿐이고 전부 부자연스러운 모양, 7종 전체 재설계해도 클래스 내부 퍼짐이 더 큼 — 회의자료_안건3_손모양거리계산표). → **해결(2026-09-22): 특징 공학**. 좌표 63차원 대신 관절 각도·거리 23차원(joint23)으로 바꿔 **정답률 77.57% → 88.68%**, 해당 쌍 2,530건 → 781건. 손모양은 그대로 두었다. 근거 Aiman & Ahmad(2023), 05_모델카드_v3 §3-5 | 해결 |
| **(신규)** 공개 데이터 내 **중복·연속 촬영 컷으로 인한 평가 누수** | 확정(실측) | 정답률이 부풀려져 KPI·τ를 잘못 고름 | 2026-09-21 실측: 같은 모델·데이터로 분할 방식만 바꾸니 **무작위 98.81% → 파일 그룹 98.88% → 세션 그룹 78.59%**(-20.2%p). 전체의 97.7%가 805개 세션에 묶여 있고 표본 69.8%에 유사도 0.999 이상 쌍둥이 존재. → 학습 스크립트(`training/train_svm.py`) 기본 분할을 **세션 단위 GroupKFold**로 고정. 공개 데이터 점수는 KPI 근거로 쓰지 않는다(04 §4) | 방침 확정 |
| ~~손 방향 축을 공개 데이터로 켜면 촬영 각도를 학습할 위험~~ | - | - | **해결(2026-09-22): 방향 축을 쓰지 않기로 확정.** 자체 촬영에서 손을 좌우로 기울여도 정답률 100%(정면 14/좌 14/우 7시도)로, 회전 정렬이 방향을 지우는 것이 오히려 강점으로 작동했다. 켤 근거가 없으므로 보류. 코드는 `--features landmark63+orient6` 로 남겨둔다 (05 §3-5, MODEL_TRAINING §2-1) | 해결 |
| ~~자체 촬영 미완료 시 KPI 측정 불가~~ | - | - | **해결(2026-09-22)**: 촬영자 2명(JH·me01) 105시도 확보. `record_dataset.py`로 촬영, `evaluate_kpi.py`로 계산. **KPI 5개 항목 전부 목표 달성** — 정답률 97.1% / 오분류 0.0% / 미판정 2.9% / MacroF1 0.985 / 치명 0건 (05 §7-3) | 해결 |
| **(신규)** KPI 달성이 **통계적으로는 확정이 아니다** | 중간 | 결과보고서에 과신한 수치를 실으면 반박 여지 | 105시도에서 오분류 0건이어도 **95% 신뢰 상한은 3.5%** 로 KPI 목표 3%를 넘는다. 즉 "강한 정황"이지 확정이 아니다. → 보고 시 신뢰 상한을 함께 기재한다. 오분류 0건을 유지한 채 약 120시도까지 늘리면 상한이 3% 아래로 내려간다(촬영 추가 시 `evaluate_kpi.py` 재실행) | 확인 중 |
| **(신규)** τ만으로는 **7종이 아닌 손모양을 거부할 수 없다** | 확정(실측) | 애매한 자세를 자신 있게 오판정 → 교육 피드백이 엉뚱해짐 | 분류기 확률은 7종 안에서 정규화돼(합=1) 낯선 입력도 "그나마 비슷한" 클래스가 높은 값을 받는다. 실측: 학습에 없던 손모양의 83%가 τ 통과, 그중 77%가 confidence 0.90 이상. 분리 AUC는 confidence 0.594(거의 무작위) vs 중심 거리 0.932. → **소속 게이트 도입**(05 §3-7). 자체 촬영으로 동작점 재조정(p1→p10): 애매한 자세 차단 38% → **75%**, 정상 자세 오거부 2.9% | 해결 |
| **(신규)** 게이트로도 못 막는 25% — **정지와 기하학적으로 매우 닮은 자세** | 중간 | 손가락 4개만 편 자세 등이 정지로 판정될 수 있음 | 새어나간 애매한 자세의 중심 유사도가 0.991로, 거리 기반으로는 원리적으로 가를 수 없다. 임계를 p16까지 올리면 100% 막히지만 정상 자세도 31%가 튕긴다. → negative 데이터를 모아 8클래스로 학습(회의 안건 2 B안) 전환 검토. 팀원 촬영분에 `--ambiguous` 포함 요청함 | 확인 중 |
| **(신규)** **측정 방법이 결과를 좌우한다** | 확정(겪음) | 잘못된 수치로 잘못된 의사결정 | 2026-09-22: `webcam_check.py`에서 숫자키로 라벨을 찍고 수백 프레임을 연속 기록한 로그로 정답률 28.2%가 나와 모델을 의심했다. 원인은 **전환 프레임이 전부 그 라벨로 기록된 것**("정지" 두 구간의 특징 중심 유사도 0.54). 같은 손을 `record_dataset.py`(카운트다운 후 3프레임)로 다시 찍으니 100%. → **KPI 측정은 반드시 `record_dataset.py`로 한다.** 연속 기록 로그는 진단용으로만 쓴다 | 방침 확정 |
| **(신규)** 🔴 **picar 주행 종료 시점이 계약에 없음 → 무한 주행** | 확정(설계 결함) | 시연 중 차가 벽·사람 쪽으로 계속 주행. 안전 사고 위험 | `picar_command.schema.json`에 지속 시간 필드가 없어, 계약대로면 다음 명령이 올 때까지 주행이 계속된다. web도 정지 명령을 따로 보내지 않는다(보낼 근거가 계약에 없음). → **임시**: `controller.py`가 주행 후 2초 뒤 자동 정지(`MOTION_DURATION_S`). **항구 대책**: 스키마에 선택 필드 `duration_ms` 추가 + 생략 시 picar 기본값 적용(fail-safe). web이 stop을 따로 보내는 방식은 그 명령이 유실되면 폭주하므로 채택하지 않음(§7상 유실은 정상 시나리오). 팀 결정 대기 — `picar_주행시간_스키마_변경안.md` 안건 1 | 대응 중 |
| **(신규)** 🔴 **물리 피드백 지연 KPI(P95≤2.0초)가 현재 정의로는 달성 불가** | 높음 | KPI 미달 확정 또는 측정 기준 혼선 | "가장 늦게 **완료**되는 시점"을 문자 그대로 보면 판정(≤1.0s)+web 폴링(0.2s)+AI Hand 제스처 완료(**실측 0.80s**, 손가락 5개×150ms)+micro:bit LED 표시(1.0s) ≈ **3.0초**로 picar를 빼도 초과한다(최초 계산 4.2초 → 2026-09-23 간격·LED 단축과 실측으로 3.0초). 간격을 100ms 이하로 줄이면 서보 3개가 겹치고, LED를 더 줄이면 학습자가 정오답을 놓친다. → **제안**: "완료"를 "각 장치가 반응을 **시작**한 시점"으로 재정의(그러면 ~1.25초로 달성 가능 — BLE 회신 실측 26~42ms). 팀 확인 대기 — 같은 문서 안건 2. 06_테스트·평가리포트_v3 측정 방법도 결정에 맞춰야 함. **2026-09-25 첫 실측(web 없는 통합 7건)**: 정답 확정 → 마지막 장치 반응 시작 **25~47ms**, vision 판정 지연 39~72ms — "시작" 기준이면 크게 여유 | 확인 중 |
| **(신규)** 🔴 **모터 고속 구동 시 I2C 명령 유실 — 정지 실패** | **확정(실측)** | **차가 멈추지 않는다.** 시연 중 발생하면 차체·관람객 안전 문제이고, 판정 흐름과 무관하게 즉시 중단해야 한다 | 2026-09-23 바닥 부하 테스트 #10(`speed 50`)에서 "끝날 때 정지 안 됨" 관측. `get_throttled=0x0`이었으므로 **전원이 아니라 I2C 경로 문제**이며, 모터 전류가 클수록 I2C에 노이즈가 실리는 것으로 보인다. **코드가 이를 증폭했다** — `_apply_motor`가 정지 쓰기 **전에** 자동 정지 타이머를 취소해, 쓰기가 실패하면 안전망까지 사라졌다. → **조치 완료(2026-09-23)**: ①순서 반전(쓰기 성공 후 취소) ②정지 재시도 3회 ③`/health`에 `last_stop_error` 노출 ④회귀 테스트 4건(변이 검증). **속도 상한 50 준수**도 완화책이다(§4.5 실측). 남은 것은 `speed 50` 재시행 확인 (13_picar_하드웨어_검증리포트_v1 §4.1). ✅ **2026-09-25 `speed 50` 재시행에서 정상 정지**(#11). 단 **주행 명령** I2C 실패 1/72(1.4%)가 계측됨 — 정지와 달리 재시도 없음, 안전 영향은 없고(2초 자동 정지) 시연 중 picar 무반응 가능 (리포트 §5 #7) | 정지: 해결 / 주행 유실: 확인 중 |
| **(신규, 2026-09-25)** 🔴 **web 판정 타이밍 — 학습자가 AI Hand를 보기 전에 오답 처리** | 높음(실측 재현) | 학습 흐름이 성립하지 않음(따라 할 틈 없이 실패·재시범 반복), 시도 횟수 집계 왜곡, 과전압(124%) 서보의 불필요한 반복 구동 | web은 수신호 시작 시 AI Hand 시범을 하지 않고, 시범 직후 곧바로 판정한다(정답 직후엔 이전 손모양이 새 수신호의 오답으로 잡힘). → 스펙: 시범 먼저 → **보는 시간** 동안 판정 무시 → vision `/reset` → 판정, 오답은 일정 시간 유지 시에만 확정. web 없는 데모 스크립트에 먼저 반영(`--observe` 3초·`--wrong-confirm` 1초), 값 확정 후 조은수에게 전달 (11_하드웨어설계서 §10 #22) | 대응 중 |
| **(신규)** 🟢 **picar 단일 전원 계통 — 모터 노이즈/전압 강하가 RPi4B에 영향** | **낮음(벤치 실측 통과)** | 모터 기동·정지 순간 5V 레일이 끌려가면 **Pi 리셋 또는 SD 손상**, Wi-Fi 끊김 시 `picar_command` 유실 → 시연 중 picar 무반응 | 2026-09-22 결정: RPi4B를 별도 어댑터가 아니라 **차체 배터리(3S 11.1V 2200mAh) 강압 5V**로 급전한다(주행 시연에 테더링이 없어야 하므로). 같은 날 오전의 '계통 분리' 방침을 번복한 것이며, **레일 공유로 노이즈 경로가 되살아났다.** 용량은 제약이 아니다(모터 듀티 10%에서 약 3.8시간). → **벤치에서 실측**: ①모터 구동 중 `vcgencmd get_throttled`(bit 0/16) ②모터 구동 중 Wi-Fi 왕복 유지. 필요 시 **5V 레일 벌크 커패시터(470~1000µF)** 추가. 배선 규칙 반전 — 헤더 5V는 급전 경로이며 **USB-C 동시 연결 금지**(역류 방지 없음). **2026-09-23 벤치 실측: 모터 20회 반복 후 `get_throttled=0x0`, Wi-Fi 왕복 실패 0건 → 커패시터 불필요.** 단 **무부하 조건**이었으므로 바닥 주행(기동 토크)과 배터리 소모 시 재확인 필요 (11_하드웨어설계서 §7.1.2) | 대응 중(바닥 주행 재확인) |
| **(신규)** picar I2C 슬레이브 주소 미검증(`0x16` 가정) | 중간 | 주소가 틀리면 모터가 전혀 동작하지 않음 | Raspbot 오리지널은 `0x16`, **V2는 `0x2B`**. 우리 보드를 오리지널로 추정한 근거는 11_하드웨어설계서_v1 부록 A에서 초음파·부저·LED·트래킹이 전부 Pi GPIO 직결이라는 점(V2는 이들도 MCU가 I2C로 관장). → 실물에서 `i2cdetect -y 1`로 확인, 다르면 `RASPBOT_I2C_ADDR` 환경변수로 덮어쓰도록 구현해 둠. 좌/우 배선 방향도 함께 확인 필요 | 미착수 |

## 신규 리스크 추가 시

새 리스크 발견 시 아래 형식으로 표에 행을 추가하고, 상태를 `미착수 → 대응 중 → 해결/방침 확정` 순으로 갱신하세요.

```
| (리스크 설명) | (발생 가능성) | (영향) | (대응 방안) | (상태) |
```