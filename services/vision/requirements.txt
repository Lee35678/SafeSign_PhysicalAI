fastapi==0.115.0
uvicorn[standard]==0.30.6
mediapipe==0.10.14
numpy==1.26.4
scikit-learn==1.5.1

# picamera2는 여기 포함하지 않습니다: Raspberry Pi OS apt 패키지(python3-picamera2)로 설치되며
# libcamera 시스템 라이브러리에 의존해 일반 pip/Docker(x86)에서 설치되지 않습니다.
# 실물 RPi5에서 카메라 연동을 구현할 때는 apt로 설치하고, 이 서비스는 --system-site-packages venv나
# 네이티브 실행으로 돌리는 것을 권장합니다 (README 참고).
