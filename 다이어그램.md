# SafeSign PhysicalAI 프로젝트 구성 다이어그램

> 작성일 2026-09-18. 출처: [README.md](README.md), [document/10_PRD_v2.md](document/10_PRD_v2.md),
> [document/02_설계문서_v2.md](document/02_설계문서_v2.md), [document/시스템_구성도_초안.md](document/시스템_구성도_초안.md),
> [docker-compose.yml](docker-compose.yml), [document/역할 및 책임표.md](document/역할%20및%20책임표.md)

---

## 1. 하드웨어/보드 배치

picar가 주행하면 카메라도 함께 이동해버리는 문제 때문에 컴퓨트 보드를 2대로 분리했다.

```mermaid
flowchart TB
    subgraph RPI5["🖥️ Raspberry Pi 5 — 학습자 앞 고정 스테이션"]
        CAM["Pi Camera Module 3 (CSI)"]
        MICROBIT["micro:bit<br/>(USB 시리얼로 각도 수신)"]
        PCA["PCA9685 서보드라이버<br/>(I2C 슬레이브, 주소 미확인)"]
        AIHAND["AI Hand 서보<br/>(손목 LD-1501MG + 손가락 LFD-01×4)"]
        SVC_VISION["services/vision"]
        SVC_ACT["services/actuation"]
        CAM --> SVC_VISION
        SVC_ACT -->|USB 시리얼: 각도 명령| MICROBIT
        MICROBIT -->|I2C| PCA
        PCA -->|PWM ×5채널| AIHAND
    end

    subgraph RPI4B["🚗 Raspberry Pi 4B 8GB — picar 탑재 (이동)"]
        SVC_PICAR["services/picar"]
        MOTOR["모터 (전/후진, 좌/우회전)"]
        LED["LED (적색×2, 황색×2)"]
        SVC_PICAR --> MOTOR
        SVC_PICAR --> LED
    end

    SVC_VISION -->|판정 결과| SVC_ACT
    SVC_ACT -.Wi-Fi HTTP POST /picar<br/>timeout 500ms + 1회 재시도.-> SVC_PICAR

    LEARNER["🧑 학습자"] -->|수신호 동작| CAM
    AIHAND -->|시범/교정| LEARNER
    MICROBIT -->|O/X, 진행 표시(LED 매트릭스)| LEARNER
    MOTOR -->|주행 반응| LEARNER
    LED -->|점등/점멸| LEARNER
```

> ⚠️ **기존 문서와의 차이**: [document/02_설계문서_v2.md](document/02_설계문서_v2.md) §1-1과
> [services/actuation/src/aihand/controller.py](services/actuation/src/aihand/controller.py)의 TODO는
> "RPi5가 gpiozero/PCA9685로 AI Hand 서보를 **직접** 구동"하는 구조를 가정하고 있었다. 하지만 실제로는
> **RPi5(actuation) → USB 시리얼 → micro:bit → I2C → PCA9685 서보드라이버 → PWM → AI Hand 서보**
> 순으로, micro:bit가 PCA9685의 I2C 마스터이자 각도 명령의 최종 수신자다(RPi5는 I2C를 직접 잡지 않음).
> 이 다이어그램은 정정된 구조로 반영했으며, 공식 문서(02_설계문서_v2, 03_인터페이스계약서_v2)와
> `controller.py`/`microbit_protocol.md`는 아직 이전 가정(RPi5 직접 제어) 그대로이므로 함께 갱신이 필요하다.

---

## 2. 서비스 아키텍처 (docker-compose 기준)

5개 서비스로 분리, 담당자별 폴더 분리 원칙([README.md](README.md) §1).

```mermaid
flowchart LR
    USER["학습자 브라우저"] --> WEB

    subgraph WEB["web :8000 (조은수)"]
        FE["frontend (SC-01~07)"]
        SM["backend/state_machine.py"]
        FE --> SM
    end

    subgraph VISION["vision :8001 (이동혁)"]
        PERC["perception/capture.py"]
        COG["cognition/classify.py + smoothing.py"]
        PERC --> COG
    end

    subgraph ACTUATION["actuation :8002 (송승호)"]
        AIH["aihand/controller.py<br/>(servo_angles 계산)"]
        MB["microbit/bridge.py<br/>(시리얼 송수신)"]
        AIH -->|각도 명령 위임| MB
    end
    MB -->|USB 시리얼| MICROBIT_HW["micro:bit"]
    MICROBIT_HW -->|I2C| PCA_HW["PCA9685 서보드라이버"]
    PCA_HW -->|PWM ×5채널| AIHAND_HW["AI Hand 서보"]

    subgraph PICAR["picar :8003 (송승호)"]
        PCTRL["controller.py (GPIO)"]
    end

    subgraph DATA["data-tools (김지훈, profile: tools)"]
        COLLECT["collect.py"]
        INITDB["init_db.py"]
        SEED["seed_templates.py"]
    end

    SM -->|GET /latest| VISION
    SM -->|POST /command, /result, /progress| ACTUATION
    SM -->|POST /picar<br/>timeout 500ms+재시도1회| PICAR

    DB[("signdb.sqlite3<br/>(shared volume)")]
    VISION --- DB
    WEB --- DB
    DATA --- DB

    SHARED["shared/schemas/*.json<br/>(공통 인터페이스 계약)"]
    VISION -.스키마 참조.- SHARED
    ACTUATION -.스키마 참조.- SHARED
    PICAR -.스키마 참조.- SHARED
    WEB -.스키마 참조.- SHARED
```

---

## 3. 판정 파이프라인 (Perception → Cognition → Actuation)

```mermaid
flowchart TD
    A["학습자 손 (3D 공간)"] --> B["Pi Camera Module 3 (CSI)"]
    B --> C["MediaPipe Hand Landmarker (LIVE_STREAM)"]
    C --> D["21 keypoints (hand_world_landmarks)"]
    D --> E["정규화 (원점이동·스케일·회전정렬)"]
    E --> F["63차원 특징벡터"]

    F --> G["SVM(RBF) 분류기<br/>7종 + negative"]
    F --> H["Cosine similarity<br/>vs DB 템플릿"]
    TEMPLATE[("수신호 템플릿 DB<br/>(오프라인 시드)")] -.조회.-> H

    G --> I{"신뢰도 ≥ τ(0.75)?"}
    I -->|Yes| J["N=3프레임 연속 동일 확인"]
    I -->|No| K["미판정 → 재시도 유도"]
    H --> L["0~100점 (percentile 매핑)"]

    J --> M["판정 확정"]
    M --> N["AI Hand: 정답 시범/교정"]
    M --> O["micro:bit: LED 매트릭스 O/X"]
    M --> P["picar: 정지/서행/좌우회전/후진/주의 + LED"]
    L --> O
```

---

## 4. 디렉터리 구조

```mermaid
flowchart TD
    ROOT["SafeSign_PhysicalAI/"]
    ROOT --> DOC["document/<br/>기획·설계·계약 문서 (10_PRD_v2가 최신 종합본)"]
    ROOT --> SVC["services/"]
    ROOT --> SHR["shared/<br/>schemas/ (5개 서비스 공통 계약)"]
    ROOT --> DC["docker-compose.yml<br/>(5개 서비스 통합 실행)"]

    SVC --> S_VISION["vision/ ← 이동혁<br/>(Perception+Cognition) — RPi5"]
    SVC --> S_ACT["actuation/ ← 송승호<br/>(AI Hand+micro:bit) — RPi5"]
    SVC --> S_PICAR["picar/ ← 송승호<br/>(picar 컨트롤러) — RPi4B 8GB"]
    SVC --> S_WEB["web/ ← 조은수<br/>(프론트엔드+상태머신)"]
    SVC --> S_DATA["data/ ← 김지훈<br/>(데이터 수집+템플릿 DB)"]

    S_VISION --> SV1["src/perception/capture.py"]
    S_VISION --> SV2["src/cognition/classify.py, smoothing.py, normalize.py"]

    S_ACT --> SA1["src/aihand/controller.py"]
    S_ACT --> SA2["src/microbit/bridge.py"]

    S_PICAR --> SP1["src/controller.py (GPIO)"]

    S_WEB --> SW1["backend/state_machine.py (SC-01~07)"]
    S_WEB --> SW2["frontend/ (index.html, main.js, style.css)"]

    S_DATA --> SD1["src/collect.py, init_db.py, seed_templates.py"]
    S_DATA --> SD2["datasets/raw, datasets/processed"]
```

---

## 5. 담당자 ↔ RACI ↔ 서비스 매핑

[document/역할 및 책임표.md](document/역할%20및%20책임표.md) 기준.

```mermaid
flowchart LR
    SSH["송승호 (팀장)"]
    LDH["이동혁"]
    JES["조은수"]
    KJH["김지훈"]

    SSH -->|R| HW["하드웨어·로봇동작"]
    SSH -->|A| DATA_R["데이터 수집"]
    SSH -->|A| PIPE["파이프라인·판정로직"]
    SSH -->|A| PERF["성능측정"]
    SSH -->|R| REQ["요구사항·범위"]

    LDH -->|A| REQ
    LDH -->|R| PIPE
    LDH -->|A| HW

    JES -->|A| WEBR["웹"]
    JES -->|R| PERF

    KJH -->|R| DATA_R
    KJH -->|C| REQ

    HW -.구현.-> S_ACT2["services/actuation"]
    HW -.구현.-> S_PICAR2["services/picar"]
    PIPE -.구현.-> S_VISION2["services/vision"]
    WEBR -.구현.-> S_WEB2["services/web"]
    DATA_R -.구현.-> S_DATA2["services/data"]
```

---

## 6. 통신 정책 요약

```mermaid
sequenceDiagram
    participant W as web (상태머신)
    participant V as vision
    participant A as actuation
    participant P as picar (RPi4B, Wi-Fi)

    W->>V: GET /latest (폴링)
    V-->>W: judgment_result

    alt 정답 확정
        W->>A: POST /command (AI Hand)
        W->>A: POST /result, /progress (micro:bit)
        W->>P: POST /picar (timeout 500ms)
        alt 500ms 내 응답
            P-->>W: 200 OK (모터/LED 구동)
        else 타임아웃
            W->>P: 1회 재시도
            alt 재시도도 실패
                Note over W,P: picar 없이 진행 (보조 출력으로 간주)
            end
        end
    end
```

---

## 갱신 규칙

- 서비스 구조/포트가 바뀌면 → [docker-compose.yml](docker-compose.yml)과 이 파일의 §2, §4를 함께 갱신
- 하드웨어 배치가 바뀌면 → [document/시스템_구성도_초안.md](document/시스템_구성도_초안.md)(원본)과 이 파일의 §1을 함께 갱신
- 수신호/판정 로직이 바뀌면 → [document/02_설계문서_v2.md](document/02_설계문서_v2.md) §4(원본)와 이 파일의 §3을 함께 갱신
